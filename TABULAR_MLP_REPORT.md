# Tabular MLP 전환 & CatBoost 블렌딩 보고서

CatBoost 기반 파이프라인을 PyTorch Tabular MLP로 전환하고, 이후 점수를 올리기 위해 시도한 방법론들(아키텍처 튜닝, 앙상블, TabM 프로토타입, CatBoost 블렌딩)을 정리한 보고서입니다. 세부 실험 로그는 `EXPERIMENTS.md`에, 코드/파이프라인 구조 설명은 `CLAUDE.md`에 있습니다 — 이 문서는 그 둘을 관통하는 흐름과 의사결정 근거, 그리고 각 시도의 실측 결과를 요약합니다.

## 요약

| 항목 | 내용 |
| --- | --- |
| 전환 계기 | Colab 프로토타입이 환산 ~1,500점을 기록했다는 보고 |
| 전환 계기의 실체 | **무효**. 무작위 20% 분할 폴백으로 인한 선수-정체성 리크였음 (§2) |
| MLP 자체 튜닝 결과 | 660.61 → **789.58** (7-seed 앙상블), CatBoost(818.54)는 못 넘음 (§3) |
| 실측 리더보드 vs 로컬 검증 | **불일치** — 로컬은 CatBoost 우위, 실제 대시보드는 MLP가 CatBoost보다 +10점 우위 (§4) |
| TabM 프로토타입 | 시도했으나 저조(597~608점), 근본 원인 미해결로 보류 (§5) |
| **최종 채택 모델** | **CatBoost + MLP 앙상블 블렌드** (가중 평균, alpha≈0.59) (§6) |
| 로컬 실측 점수 (season==2024 홀드아웃) | **851.19** (CatBoost 818.54, MLP 789.58 대비 각각 +32.65 / +61.61) |
| **실제 대회 대시보드 점수** | **924** (로컬 대비 +72.81 — §4의 "로컬이 실전을 과소평가하는 패턴"이 블렌드에서도 재현, 오히려 더 크게 벌어짐) |
| 수료 기준 | 549.51 (여유 있게 통과) |
| 제출 상태 | MLP 단독 버전 제출 완료 → 이후 블렌드 버전(전체 데이터 재학습본)으로 재제출 완료, 대시보드 924점 확인 |

## 1. 왜 CatBoost에서 MLP로 전환했나

기존 CatBoost 파이프라인은 피처 엔지니어링(파생 피처 12개) + Optuna 하이퍼파라미터 튜닝(40 trials)을 거쳐 **BSS 0.00819 / 환산 818.54점**까지 개선된 상태였습니다(`EXPERIMENTS.md` 1~4장).

이 상황에서 사용자가 Colab에서 다음과 같은 훨씬 단순한 설정으로 PyTorch MLP를 돌려본 결과를 공유했습니다:

- 피처셋: `test.csv` 원본 47개 컬럼만 사용 (트랙맨 병합·파생 피처 없음)
- 범주형: `top_bottom`/`game_type`/`base_state` 3개만 `nn.Embedding` (embed_dim=8 고정)
- 나머지 전부(선수/팀 ID 포함) 숫자로 스케일링
- 구조: `Embedding → Linear(128)→BN→ReLU→Dropout(0.3)→Linear(64)→BN→ReLU→Linear(1)→Sigmoid`
- 3 epoch, `season==2024` 홀드아웃(의도)

이 코드가 **환산 1,000점을 넘겼다**는 보고를 받고, 기존 트랙맨 병합/파생 피처 파이프라인은 그대로 유지한 채 모델만 CatBoost → Tabular MLP로 전면 교체했습니다.

## 2. Colab 1,500점의 진실 — 무효한 벤치마크였다

실제 데이터(`open/data/`, train.csv 1,475,092행)로 동일 구조의 MLP를 전체 파이프라인(트랙맨 병합 + 파생 피처 포함)에 적용해 처음 학습시킨 결과는 **BSS 0.00661 / 환산 660.61점**으로, Colab의 1,500점과 큰 괴리가 있었습니다. 원인을 다음 순서로 규명했습니다.

1. **트랙맨 데이터 제외 비교**: 트랙맨 병합을 완전히 뺀 버전 → 626.66점. 포함 버전(660.61)이 근소하게 더 높아, 트랙맨이 점수를 깎는 원인이 아님을 확인.
2. **시간 필터(Time Filter) 재확인**: `process_trackman_features_safe`의 `is_train_split=True` 안전장치가 실제로는 아무 행도 제거하지 않음을 확인(`train.csv`·`trackman_history.csv` 둘 다 2024년 최대 `game_month`=10로 동일). 그러나 병합 키(`match_cols`)에 이미 `season`/`game_month`가 포함되어 있어 애초에 시즌을 넘나드는 병합 자체가 불가능 — 리크 없음, 괴리의 원인 아님.
3. **Colab 코드 재검토 (사용자 확인 완료)**: Colab 코드에는 `if val_data.shape[0] == 0:` 일 때 무작위 인덱스로 20%를 분할하는 폴백이 있었고, 실제로 이 폴백이 트리거되어 `season==2024` 시간 기준 홀드아웃이 아니라 **무작위 20% 분할**로 검증되었던 것으로 확인됨. 무작위 분할 + `pitcher_id`/`batter_id`/팀 ID를 원본 숫자로 그대로 넣는 조합은 동일 선수의 다른 투구가 train/val 양쪽에 섞여 들어가는 **선수-정체성 리크**를 유발하기 쉬운 조합임.

**결론: Colab의 1,500점은 무효한 벤치마크였다.** 이후 목표는 "1,500점 재현"이 아니라, 리크 없는 `season==2024` 홀드아웃 기준으로 **CatBoost(818.54)를 최대한 따라잡는 것**으로 재설정했습니다.

## 3. MLP 자체를 개선하기 위해 시도한 방법론들

기준선(660.61)에서 출발해 시도한 방법을 순서대로 정리합니다.

### 3.1 범주형 컬럼 확장 (실패)

CatBoost의 feature importance 상위권이 `game_type`(25.4%), `season`(16.3%)이었던 점에 착안해, 임베딩 대상 범주형 컬럼(`CAT_COLS`)을 3개에서 10개로 확장(`season`, `pitcher_hand`, `batter_hand`, `pitcher_team_id`, `batter_team_id`, `pitcher_id`, `batter_id` 추가)하고, 컬럼별 카디널리티에 비례한 임베딩 크기(fastai 스타일 휴리스틱, `min(50, (card+1)//2)`)를 부여했습니다.

**결과: 660.61 → 303.79로 급락.** `best_epoch=3`에서 조기 종료되는 등 명백한 과적합 패턴이 나타났습니다. 원인을 분리 진단한 결과:

- **`pitcher_id`(792)/`batter_id`(830)**: 학습 데이터(~122만 행) 대비 카디널리티가 높아, 표본이 적은 개별 선수의 임베딩이 일반화되는 신호 대신 노이즈를 암기해버림. CatBoost는 정렬 기반 target statistic으로 고카디널리티 카테고리를 정규화해서 다루지만, MLP의 학습형 임베딩은 그런 안전장치가 없음. 게다가 `asof_pitcher_*`/`asof_batter_*` 누적 성공률 피처가 이미 선수 정체성 정보를 간접적으로 담고 있어, 원본 ID를 추가로 넣는 실익도 적음. 이 둘만 제외해도 351.53으로 여전히 낮아, 더 큰 원인이 따로 있음을 시사.
- **`season`**: 검증셋(2024)이 학습셋(2019~2023)에 **아예 존재하지 않는 시즌값**이라는 점이 핵심. `season`을 임베딩으로 두면 검증 데이터 전원이 "미학습(unknown) 카테고리"의 동일한 랜덤 임베딩을 공유하게 되어, 예측 전체가 체계적으로 miscalibrate됨. 이는 검증 실험만의 문제가 아니라 **실제 제출 환경에도 그대로 적용되는 문제**입니다 — 평가 서버의 `test.csv`는 2025 시즌으로, `train.csv`(2019~2024)에도 없는 값이기 때문입니다.

  → **일반화 가능한 교훈**: 미래에 반드시 새로운 값이 나타나는(시간에 따라 순서를 갖는) 컬럼은 임베딩이 아니라 숫자(스케일링)로 다뤄야 외삽이 가능하다.

`season`만 다시 제외하자(`pitcher_id`/`batter_id`/`season` 모두 제외, `pitcher_hand`/`batter_hand`/`pitcher_team_id`/`batter_team_id`만 임베딩 유지, 총 7개 `CAT_COLS`) **690.62점**으로 회복 — 원래 기준선(660.61)보다도 소폭 개선.

### 3.2 하이퍼파라미터 스윕 (유의미한 개선 없음)

7개 `CAT_COLS` 구성을 기준으로 추가 탐색:

| 설정 | 점수 | 비고 |
| --- | --- | --- |
| 기준 (lr=0.003, weight_decay=0.01) | 690.62 | — |
| lr=0.001 | 647.43 | 하락 |
| weight_decay=0.05 | 684.66 | 유의미한 차이 없음 |

단일 모델은 **같은 설정으로도 epoch마다 검증 점수가 크게 흔들리는** 것을 관찰했습니다(같은 run 안에서 Val Score가 490~690점대를 오르내림). 하이퍼파라미터를 바꾸는 것보다, 이 **분산 자체**가 병목이라고 판단해 방향을 전환했습니다.

### 3.3 시드 앙상블 (Deep Ensemble — MLP 자체 개선의 최종 채택안)

서로 다른 무작위 시드로 학습한 완전히 독립적인 모델 여러 개(**Deep Ensemble** — 가중치를 전혀 공유하지 않는 별도 모델들, packed ensemble이나 BatchEnsemble류의 효율적 앙상블과는 다름)의 예측 확률을 평균하는 방식으로 분산을 줄였습니다. 전처리기(OrdinalEncoder/Imputer/Scaler)는 1회만 fit하고, 시드별로 가중치 초기화와 배치 순서만 다르게 학습합니다.

| 설정 | 점수 | 비고 |
| --- | --- | --- |
| 단일 모델 최고치 (참고용) | 715~740 | 시드에 따라 편차 큼 |
| **7-seed 앙상블** | **780.92** (ad-hoc 실험) / **789.58** (실제 파이프라인 실측) | **MLP 단독 최종 채택** |
| 15-seed 앙상블 | 768.93 | 추가 개선 없음, 정체 |
| 7-seed 앙상블 + 네트워크 확장(256/128, dropout 0.4, patience 10) | 777.92 | 개선 없음 |

7개 이상으로 시드를 늘리거나 네트워크를 키워도 780~790점대에서 정체되는 것을 확인하고, 계산 비용이 가장 적은 **7-seed, 원래 크기 네트워크(128/64)** 조합을 MLP 단독 최종안으로 채택했습니다 (이후 §6에서 CatBoost와 블렌딩하는 구성 요소가 됩니다).

**MLP 최종 아키텍처**: `nn.Embedding`(컬럼별 크기, fastai 휴리스틱) → `Linear(128)→BatchNorm→ReLU→Dropout(0.3)→Linear(64)→BatchNorm→ReLU→Linear(1)→Sigmoid`, `BCELoss` + `AdamW(lr=0.003, weight_decay=0.01)`, batch_size=4096, early stopping patience=7(최대 60 epoch), `CAT_COLS = ['top_bottom','game_type','base_state','pitcher_hand','batter_hand','pitcher_team_id','batter_team_id']`, `ENSEMBLE_SEEDS = [42, 123, 7, 2024, 99, 555, 31337]`.

## 4. 실측 리더보드 vs 로컬 검증 — 결과가 뒤집혔다 (그리고 격차가 더 벌어졌다)

로컬 `season==2024` 홀드아웃 기준으로는 CatBoost(818.54)가 MLP 앙상블(789.58)보다 29점 가까이 높았습니다. 이 결과를 바탕으로 MLP 단독 버전을 먼저 제출했는데, **실제 대회 대시보드에서는 MLP 앙상블이 CatBoost보다 오히려 약 10점 더 높게** 나왔습니다 — 로컬 검증과 실제 순위가 뒤바뀐 것입니다.

**추정 원인** (확실하지는 않으나 유력한 설명): CatBoost의 하이퍼파라미터는 `season==2024` 홀드아웃을 목표로 한 40회 Optuna 탐색으로 얻어졌습니다. 이 과정에서 2024 시즌 고유의 패턴에 과적합되었을 가능성이 있습니다. 반면 MLP 앙상블은 시드 평균을 통한 분산 완화(§3.3) 덕분에, 로컬 홀드아웃 점수는 더 낮더라도 완전히 새로운 데이터(실제 평가 시즌인 2025)에는 오히려 더 잘 일반화되었을 수 있습니다.

**블렌드 제출 이후 다시 확인됨 (§6.3)**: CatBoost+MLP 블렌드도 같은 패턴을 보였습니다 — 로컬 851.19 대비 **실제 대시보드 924**, 로컬을 +72.81 앞섭니다. MLP 단독 사례(+10점 차이)보다 오히려 격차가 더 커졌습니다. 우연한 역전 1회가 아니라, 로컬 `season==2024` 홀드아웃이 실전(`season==2025`) 성능을 **체계적으로 과소평가**하는 경향임을 시사합니다.

**적용 방침**: 이후로는 로컬 홀드아웃과 실제 대시보드 결과가 상충할 경우 **실제 대시보드를 신뢰**합니다. 로컬 홀드아웃 점수를 과도하게 최적화(예: 홀드아웃에 대한 추가 Optuna 탐색)하는 것은 CatBoost가 겪은 것과 같은 과적합 함정에 빠질 위험이 있다고 보고 경계합니다. 로컬 점수는 후보 모델 간 **상대적 비교**에는 여전히 유용하지만, 절대적인 실전 성능 추정치로는 신뢰하지 않습니다.

## 5. TabM 프로토타입 — 시도했으나 보류 (이후 §5.1에서 재도전, 종결)

Deep Ensemble(§3.3)의 비용(7배 학습 시간·저장 공간) 없이 비슷한 분산 완화 효과를 노려, BatchEnsemble/TabM 스타일의 **효율적 앙상블**(공유 백본 가중치 + 멤버별 rank-1 승수 벡터 `r_k`/`s_k`)을 자체 구현해 프로토타입 검증했습니다.

- **구조**: 카테고리 임베딩은 공유, 각 `Linear` 층을 `EnsembleLinear`(가중치 W 공유, 멤버별 `r_k`(입력 스케일)·`s_k`(출력 스케일))로 교체. k=32 가상 멤버를 단일 학습 run으로 동시에 학습.
- **1차 시도** (r/s를 N(1, 0.1)로 초기화, 일반 weight decay 적용): 로컬 점수 **607.82** — 단일 MLP(690.62)보다도 낮음. `best_epoch=7`로 조기 종료, 과적합 양상.
- **가설(weight decay가 승수 벡터를 0쪽으로 당겨 앙상블 다양성을 억누른다)을 검증하기 위해 r/s를 weight decay 대상에서 제외** → 2차 시도 **597.32**로 사실상 변화 없음. 가설 기각.
- **잔여 가설**: r/s를 항등에 가깝게(N(1,0.1)) 초기화한 탓에, 32개 가상 멤버가 초기에 거의 동일하게 시작해 (과적합이 시작되는 이른 시점, ~epoch 7) 까지 충분히 분화하지 못했을 가능성. 원 BatchEnsemble 논문의 Rademacher(±1) 초기화 등 더 공격적인 초기화가 필요할 수 있음.

### 5.1 재도전 — Rademacher(±1) 초기화로 잔여 가설 검증 (종결)

CatBoost×MLP 스태킹 채택(§6~§9) 이후, 두 모델 간 이질성을 늘릴 방법을 모색하던 중 TabM의 잔여 가설을 재검증. 기존 코드는 남아있지 않아 재구현하고, r/s 초기화만 Rademacher(±1)로 교체(BatchNorm은 k개 가상 멤버 통계를 하나의 super-batch로 섞어버리는 문제가 있어 제외):

| 시도 | 로컬 점수 |
| --- | --- |
| 1차 (N(1,0.1) 초기화) | 607.82 |
| 2차 (weight decay 제외) | 597.32 |
| **3차 (Rademacher(±1) 초기화)** | **708.39** (+100.57, best_epoch=5) |

잔여 가설은 **맞았음** — 초기화를 바꾸는 것만으로 큰 폭 개선. 다만 Deep Ensemble(777~790)에는 여전히 못 미치고, CatBoost와 스태킹해도(832.83) 현재 프로덕션(CatBoost+Deep Ensemble MLP 스태킹, 855~861)보다 낮음. **TabM 항목은 이걸로 종결 — 프로덕션 미채택.** 학습 비용 절감이라는 원래 동기는 유효하지만 이 데이터셋에서는 정확도 손실이 더 큼. 상세 실험 로그는 `EXPERIMENTS.md` §11 참고.
- **보류 사유**: 2번째 수정도 실패한 시점에, 마침 §4에서 로컬 검증 자체의 신뢰도 문제가 확인되어 — 로컬 점수를 더 파는 것의 기대 가치가 낮아졌고, 같은 시간에 시도한 CatBoost 블렌딩(§6)이 훨씬 빠르고 확실한 개선을 보여 우선순위에서 밀렸습니다. **미해결 상태로 남겨둠** — 향후 재시도 시 초기화 방식부터 바꿔볼 것.

## 6. CatBoost + MLP 블렌딩 — 채택된 최종 해법

### 6.1 아이디어와 첫 검증

서로 다른 계열의 모델(트리 기반 GBDT vs 신경망)을 섞으면 오차 상관관계가 낮아 앙상블 효과를 볼 수 있다는 가설을 세우고, 이미 학습되어 있던 두 모델(CatBoost 818.54, MLP 앙상블 789.58)의 예측 확률을 `alpha * CatBoost + (1-alpha) * MLP` 형태로 가중 평균하며 alpha를 0~1 그리드 서치했습니다. 새 학습이 전혀 필요 없어 수 분 내에 검증 가능했습니다.

**결과 (ad-hoc, 기존 모델 재사용)**:

| alpha (CatBoost 비중) | 점수 |
| --- | --- |
| 0.00 (MLP 단독) | 789.58 |
| 0.55 (최적) | **854.09** |
| 1.00 (CatBoost 단독) | 818.54 |

곡선은 alpha=0.55~0.60 부근에서 매끄러운 단봉(unimodal) 형태의 최고점을 보여 우연이 아님을 확인했습니다. 두 모델의 오차 상관계수는 0.9991로 매우 높았음에도(보통 블렌딩 효과를 제한하는 요인) 유의미한 개선이 있었던 점이 특히 인상적이었습니다.

### 6.2 파이프라인 반영

정식으로 파이프라인에 반영했습니다 (재현성을 위해 CatBoost 하이퍼파라미터는 기존 튜닝된 reference 모델의 `get_params()`에서 그대로 추출 — 이는 대회 규칙상 문제없음: 이 하이퍼파라미터 자체가 공식 제공 데이터만으로 수행한 팀 자체 Optuna 탐색 결과이므로 외부 데이터/모델을 끌어오는 것이 아님).

- **`code/catboost_model.py`** (신규): `CATBOOST_PARAMS`(튜닝된 하이퍼파라미터 하드코딩), `train_catboost()`(검증셋 있으면 BrierScore 기준 early stopping, 없으면 고정 iteration), `predict_catboost()`.
- **`code/blend_model.py`** (신규): `sweep_alpha()`(0.01 간격 grid search로 최적 blend 비중 탐색), `make_blend_bundle()`, `predict_blend_bundle()`.
- **번들 스키마 변경**: `{"catboost_model": CatBoostClassifier, "mlp_bundle": {...기존 MLP 앙상블 번들...}, "alpha": float, "catboost_best_iteration": int}`. `catboost_model`은 프로젝트 전용 클래스가 아니라 `catboost` 라이브러리 클래스라 `code/` 패키지 없이도 (catboost만 설치되어 있으면) 정상 unpickle됨.
- **`code/train.py`**: MLP 앙상블 학습 후 동일한 train/val split으로 CatBoost도 학습, 두 모델의 val 예측으로 `sweep_alpha()` 수행 후 블렌드 번들을 `open/temp/latest_model.pkl`에 저장.
- **`code/test.py`**: reference 번들에 `"catboost_model"`+`"mlp_bundle"` 키가 모두 있어야 블렌드 포맷으로 인식하고 비교, 없으면(CatBoost 단독/MLP 단독 시절 레거시) 비교 없이 신규 모델을 채택 — 기존 self-migration 관례와 동일.
- **`dopip.py` Full Retrain**: reference의 alpha·CatBoost `best_iteration`·MLP 멤버별 `best_epoch`를 그대로 재사용해, 전체 데이터(2019~2024)로 CatBoost와 MLP 7-seed 앙상블을 각각 재학습 후 같은 alpha로 블렌드 번들 구성.
- **`submit/script.py`** (독립 실행 파일): CatBoost `predict_proba`와 MLP 앙상블 평균 예측을 로드된 alpha로 블렌드. `submit/requirements.txt`에 `catboost==1.2.10` 재추가(평가 서버 기본 설치 목록에 없어 명시 필요).

### 6.3 실제 파이프라인 재학습 결과 및 실제 대시보드 점수

ad-hoc 테스트는 기존에 저장되어 있던 모델들을 재사용한 것이었고, 이후 `dopip.py`를 새로 돌려 **완전히 새로 학습한 CatBoost + MLP 조합**으로 재검증했습니다:

| 단계 | 데이터 범위 | alpha | 로컬 점수 |
| --- | --- | --- | --- |
| 평가용 (season<2024 학습, 2024 검증) | 2019~2023 | 0.59 | 851.19 |
| 최종 제출용 (Full Retrain) | 2019~2024 전체 | 0.59 (재사용) | *(홀드아웃 없어 직접 측정 불가)* |

alpha가 ad-hoc 실험(0.55)과 실제 파이프라인(0.59) 사이에서 근소하게 다른 것은 CatBoost/MLP를 완전히 새로 학습했기 때문이며, 두 값 모두 0.5~0.6 구간에 안정적으로 위치합니다.

두 모델 모두 스탠드얼론 `submit/script.py` + 실제 데이터로 스모크테스트를 통과했습니다 (추론 시간 약 10초, 5개 샘플 행 기준).

**최종 제출본(Full Retrain)을 실제 대회 대시보드에 제출한 결과: `924점`.** 로컬 평가용 점수(851.19) 대비 +72.81 — §4에서 확인한 "로컬이 실전을 과소평가하는 패턴"이 블렌드에서도 재현되었고, 오히려 MLP 단독 사례(+10점)보다 더 크게 벌어졌습니다. CatBoost 단독(818.54) 대비로는 실질적으로 100점 이상의 개선입니다.

## 7. 최종 결과 비교

| 모델 | 실측 BSS 점수 (`season==2024` 홀드아웃) | 비고 |
| --- | --- | --- |
| CatBoost (피처 엔지니어링 + Optuna 튜닝) | 818.54 | — |
| Tabular MLP 단일 모델 (최초 마이그레이션, `CAT_COLS` 3개) | 660.61 | — |
| Tabular MLP 단일 모델 (`CAT_COLS` 3개, 트랙맨 제외) | 626.66 | — |
| Tabular MLP 단일 모델 (`CAT_COLS` 10개 — 실패한 시도) | 303.79 | — |
| Tabular MLP 단일 모델 (`CAT_COLS` 7개, season/ID 제외) | 690.62 | — |
| Tabular MLP 7-seed 앙상블 | 789.58 | 실제 리더보드는 CatBoost보다 +10점 우위 (§4) |
| TabM 프로토타입 (k=32, N(1,0.1) 초기화) | 597~608 | 보류 (§5) |
| TabM 프로토타입 (k=32, Rademacher(±1) 초기화 재도전) | 708.39 | 가설 확인됐으나 Deep Ensemble 대체는 못함, 종결 (§5.1) |
| CatBoost + MLP 블렌드 (alpha≈0.59, 로컬) | 851.19 | CatBoost 대비 +32.65, MLP 대비 +61.61 |
| **CatBoost + MLP 블렌드 — 실제 대회 대시보드 (최종)** | **924** | 로컬 대비 +72.81, CatBoost(로컬) 대비 실질 +100 이상 |
| 수료 기준 | 549.51 | 여유 있게 통과 |

## 8. 얻은 교훈 (일반화 가능한 것들)

1. **벤치마크 숫자는 검증 방법론을 먼저 확인하라.** Colab의 1,500점은 코드 자체의 버그(폴백 분기)로 인한 무효 지표였다 — 절대 점수보다 "어떻게 검증했는가"가 먼저다.
2. **미래에 새 값이 나올 것이 확실한 컬럼은 임베딩하지 말라.** `season`처럼 시간에 따라 값이 늘어나는 컬럼을 범주형 임베딩으로 다루면, 검증/실전에서 반드시 마주치는 미학습 카테고리가 예측을 통째로 망가뜨릴 수 있다. 숫자로 다루면 최소한의 외삽이 가능하다.
3. **고카디널리티 식별자(ID) 임베딩은 데이터 양 대비 신중하게.** 표본이 부족한 카테고리는 MLP 임베딩이 쉽게 암기(과적합)한다. 이미 다른 피처(예: 누적 성공률)가 정체성 정보를 간접적으로 담고 있다면 원본 ID를 추가하는 실익이 작을 수 있다.
4. **단일 신경망의 분산이 병목이라면, 하이퍼파라미터보다 앙상블을 먼저 의심하라.** 학습률·정규화를 조정해도 정체됐지만, 서로 다른 시드로 학습한 모델을 평균하는 것만으로 690→790점대까지 개선됐다.
5. **앙상블도 무한정 좋아지지 않는다.** 7개에서 15개로 시드를 늘려도, 네트워크를 키워도 780~790점대에서 정체 — 어느 시점부터는 분산 감소보다 모델 자체의 표현력 한계가 병목이 된다.
6. **로컬 홀드아웃은 실전 순위를 보장하지 않으며, 절대 점수도 체계적으로 과소평가할 수 있다.** 튜닝을 홀드아웃에 직접 최적화할수록(예: Optuna로 40회 탐색) 그 홀드아웃 자체에 과적합될 위험이 커진다. 이번 프로젝트에서는 순위 역전(MLP 단독: 로컬 -29점이지만 실전 +10점)뿐 아니라, 블렌드에서도 로컬(851.19) 대비 실전(924)이 +72.81 더 높게 나오는 등 격차가 일관되게 "실전 > 로컬" 방향으로 벌어졌다 — 로컬과 실전 결과가 갈릴 때는 실전을 신뢰하고, 로컬 지표를 과도하게 파고들지 않는 것이 안전하다.
7. **서로 다른 모델 계열을 블렌딩하는 것은 가성비가 매우 높다.** 오차 상관계수가 0.999에 가까울 정도로 높았음에도, 단순 가중 평균만으로 두 모델 각각의 단독 성능을 모두 훌쩍 뛰어넘었다 (818.54/789.58 → 851.19). 새로운 학습이 전혀 필요 없어 검증 비용도 거의 0에 가까웠다 — 아키텍처를 붙잡고 며칠씩 튜닝하는 것보다 먼저 시도해볼 가치가 있는 저비용·고효율 기법.
8. **효율적 앙상블(BatchEnsemble/TabM류)은 초기화가 핵심이다.** 승수 벡터를 항등에 가깝게(N(1,0.1)) 초기화하면 멤버 간 분화가 느려 Deep Ensemble만 못한 결과가 나온다 — 원 논문의 Rademacher(±1) 초기화로 바꾸자 실측 +100.57점 개선되어 가설이 확인됐다(§5.1). 다만 그렇게 고쳐도 이 데이터셋에서는 여전히 Deep Ensemble을 넘지 못했다 — 원 논문 그대로 따르지 않고 축약 구현하면 성능 함정에 빠지기 쉽다는 것과, 근본 원인을 고쳐도 반드시 최선의 대안을 이긴다는 보장은 없다는 것 둘 다 보여준 사례.
9. **서로 다른 모델 "계열" 간의 이질성이 앙상블 이득을 좌우한다.** CatBoost(GBDT)+MLP(신경망) 블렌드는 오차 상관계수 0.999에도 큰 이득을 냈지만, TabM을 MLP 앙상블 대신 넣은 스태킹(CatBoost+TabM, 832.83)은 CatBoost+Deep Ensemble MLP 스태킹(855~861)보다 낮았다 — TabM도 결국 신경망 계열이라 CatBoost와의 상관관계(0.8835)가 Deep Ensemble MLP와 별 차이가 없었기 때문. 이질성은 "다른 알고리즘을 쓴다"가 아니라 "실제로 다르게 틀린다"에서 나온다.
10. **이질성만으로는 스태킹 이득이 보장되지 않는다 — 단독 성능이 뒷받침돼야 한다. 그리고 튜닝이 오히려 역효과를 낼 수도 있다.** CatBoost+MLP 2-way 스태킹에 LightGBM/XGBoost/RandomForest를 세 번째 모델로 추가해본 결과(`EXPERIMENTS.md` §12), 상관관계가 가장 낮았던(가장 이질적인) RandomForest(0.830/0.806)가 오히려 스태킹을 가장 크게 깎아먹었다(-6.36) — 단독 성능이 너무 약해서(521.88) 다양성 이득보다 노이즈를 더 끌고 들어왔기 때문. 튜닝 없는 XGBoost(0.915 상관, 729.46)는 유일하게 순이득을 냈지만(+7.36, rolling-origin 재검증 2/3·+5.07로 §9.1 채택 기준(3/3, +14.22) 미달), 팀원이 Optuna 55 trial로 실제 튜닝한 훨씬 강한 XGBoost(로컬 752.70, 이 저장소에서 재현 시 744.38)를 그대로 넣었더니 **오히려 더 나빠졌다**(rolling-origin 1/3·평균 -7.55, §12.2) — 단독 성능이 오르면서 CatBoost와의 상관관계도 같이 올라가(0.915→0.934) 이질성 이득이 상쇄된 것으로 추정. "더 강한 모델을 넣으면 스태킹도 더 좋아질 것"이라는 직관이 이번엔 틀렸다.
11. **rolling-origin CV의 극단적인 fold 결과는 "그 시점의 특성"이 아니라 "표본 부족에 의한 메타모델 불안정"일 수 있다.** §10의 튜닝 XGBoost가 5~6월 fold에서 보인 -28.37이라는 큰 손해는, 메타모델을 그 fold의 학습 표본(3~4월치, 5.5만 행)만으로 학습시켰기 때문이었다 — 상관관계가 높은(0.93+) 피처들 사이에서 로지스틱 회귀가 표본이 작을 때 계수 배분을 잘못했고(`w_mlp`가 0.29로 붕괴), 학습 표본을 3배로 늘리자(5~6월만 제외한 나머지 전체) `w_mlp`가 정상 회복되며 손해도 -28.37→-7.09로 대부분 줄었다(`EXPERIMENTS.md` §13.1). 실제 프로덕션은 메타모델을 검증셋 전체로 한 번에 학습시키므로 이 정도 소표본 불안정 자체가 재현되지 않는다 — CV로 안정성을 검증할 때 극단적인 fold는 그 fold의 학습 표본 크기부터 의심해볼 것.
12. **모델의 "약점"이 항상 그 모델의 한계인 것은 아니다 — 학습 목적함수가 평가지표와 안 맞아서일 수도 있다.** RandomForest(sklearn `RandomForestClassifier`, 기본 Gini 불순도 기준)의 단독 성능(521.88)이 유독 낮았던 것은 배깅 자체의 한계라기보다, 학습에 쓰는 목적함수(Gini/log_loss, 클래스 순도 기준)가 평가지표(Brier score, 확률 보정 기준)와 어긋나 있었기 때문이었다 — `RandomForestRegressor`를 0/1 타겟에 그대로 학습시켜(Brier와 수학적으로 동일한 MSE를 직접 최소화) 521.88→667.94(+28%)까지 끌어올렸다(`EXPERIMENTS.md` §13.2). 반대로 "용량을 늘리면(더 깊은 트리) 나아지지 않을까"라는 직관적인 방향은 시도해보니 오히려 나빠졌다(521.88→212.41) — 문제의 원인을 정확히 진단하지 않고 "더 크게/더 복잡하게"부터 시도하면 엉뚱한 방향으로 갈 수 있다는 사례이기도 하다. 다만 이렇게 개선된 RandomForest도 3-way 스태킹 채택 기준은 넘지 못했다(rolling-origin 평균 -2.75~-2.93) — 목적함수 정합성 문제를 고쳐도 반드시 최선의 대안을 이긴다는 보장은 없다는 점에서 §8의 TabM 사례와 같은 결.

## 9. 다음으로 시도해볼 만한 방향

- ~~**TabM 재도전**~~: 완료·종결 (§5.1). Rademacher 초기화로 가설은 확인됐으나 Deep Ensemble을 대체할 만큼은 아니라 프로덕션 미채택.
- **수치형 피처 임베딩(numerical embedding)**: MLP가 GBDT의 분기 표현력을 따라잡기 위한 최신 기법(periodic/quantile embedding 등) — 시도해보지 않음.
- **트랙맨 피처 고도화**: `asof_batter_*` 계열이 상대적으로 덜 활용되어 있고, 트랙맨 쪽 pitch-mix/상황 교차 피처도 아직 없음(`EXPERIMENTS.md` 참고, CatBoost 쪽에서 먼저 논의됨).
- ~~**블렌딩 확장 — 소규모 스태킹(메타 모델)**~~: 완료·프로덕션 반영 (`EXPERIMENTS.md` §9).
- ~~**블렌딩 확장 — 세 번째 모델 계열 추가**~~: LightGBM/XGBoost/RandomForest 시도, 종결·보류 (`EXPERIMENTS.md` §12~§13). 튜닝 없는 XGBoost만 약한 방향성이 있었으나(2/3 fold, 평균 +5.07) 채택 기준 미달, 팀원의 실전 튜닝된 XGBoost(로컬 752.70)는 오히려 더 나빠짐(§12.2). RandomForest는 objective를 Brier(MSE)로 바꿔 단독 성능을 28% 끌어올렸지만(§13.2) 그래도 3-way 채택 기준은 못 넘음. GBDT/배깅 계열을 더 파는 것보다, 진짜 이질적인 모델(예: 선형/커널 계열)을 시도하는 쪽이 남은 여지가 있다면 더 유망해 보이나 우선순위는 낮춤.

---

*작성 기준: 실측 데이터(`open/data/`, train.csv 1,475,092행) 기준. 전체 실험 로그는 `EXPERIMENTS.md`, 코드/파이프라인 구조는 `CLAUDE.md` 참고.*
