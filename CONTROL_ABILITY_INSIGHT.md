# 제구력이란 무엇인가 — 모델 변천사로 역산한 도메인 정의

> LG Aimers Phase 2 (야구 투구 제어 성공 예측, `control_success`, Brier Skill Score)
> 2026-08-05 베이스라인 549.51 → 2026-08-31 실전 1126.77 까지의 전체 실험 기록을
> "점수가 왜 올랐고 왜 내렸는가"의 관점에서 정리하고, 마지막에 '제구력'의 조작적 정의를 도출한다.
>
> 원자료: `PROJECT_HISTORY.md`(§1~§93), `EXPERIMENTS.md`, `CLAUDE.md` 상단 업데이트 로그, 대시보드 제출 기록.

---

## 0. 문제의 뼈대 — 무엇을 예측하는가

대회가 정의한 **제구 실패**는 세 가지뿐이다.

1. 스트라이크존 **가운데** 부근으로 들어간 공 (한복판 실투)
2. 스트라이크존에서 **크게 벗어난** 공 (폭투성)
3. 포수의 요구 방향과 **반대로** 들어간 공 (배터리 미스)

그 외 유효한 투구는 전부 제구 성공(`control_success=1`). 즉 타깃은 **"공의 최종 위치 × 포수 요구 방향"** 으로만 정의되며, 구속·회전수·구위·삼진 여부는 정의에 들어가지 않는다. 이 한 문장이 이후 모든 트랙맨(구위 물리량) 실험이 실패한 구조적 이유가 된다.

제약:
- 투구 **직전**까지 확인 가능한 정보만 사용 (미래 정보·현재 투구의 결과 금지)
- `test.csv`의 각 행은 **독립적으로** 예측 (다른 행으로 누적/타깃인코딩 금지)
- 학습 데이터는 2019~2024, **평가는 2025 시즌** — train에 없는 season 값
- 지표: `BSS = max(0, 100000 × (1 − Brier / [r(1−r)]))`, r은 비공개 평균 제구율

---

## 1. 실전 리더보드 점수 타임라인 (확정 최고점만)

| 날짜 | 실전 점수 | Δ | 무엇이 바뀌었나 |
|---|---:|---:|---|
| 2026-08-07 | ~795 | — | 팀원 CatBoost 베이스라인 (수료 기준선 549.51 대비 +245) |
| 2026-08-12 | 927.99 | +133 | CatBoost 재튜닝 + **MLP 7-seed 앙상블 블렌드** + 카운트별 구종비중 |
| 2026-08-13 | 957.80 | +30 | + **Quantile(PLE) 수치 임베딩** (n_bins=24) |
| 2026-08-14 | 971.41 | +14 | + **F1 필터** (퓨처스리그 2019~2022 오염 행 제거) + 트랙맨 완전 제거 |
| 2026-08-16 | 982.22 | +11 | + **cutoff=7 split** (2024 상반기 학습 포함) + 스태킹 메타모델 |
| 2026-08-19 | 1027.54 | +45 | + **시즌 진행분 피처** (커리어 누적 → "이번 시즌만" 분해, 8개) |
| 2026-08-20 | 1041.40 | +14 | + **Track A TE-residual** (상황별 target-encoding 잔차, CatBoost 전용) |
| 2026-08-28 | 1092.998 | +52 | **유담 레시피 전면 포팅** (트랙맨 상황조인 복원 + reverse_rate + v2 HP + raw MLP) |
| 2026-08-28 | 1117.03 | +24 | **트랙맨64 상황조인 완전 제거** (2025 추론 시 상수 붕괴) |
| 2026-08-31 | **1126.77** | +9.7 | **team_id를 CatBoost categorical로 선언** (cat_team) — 현재 확정 최고 |

베이스라인 549.51 → 1126.77, 순증 **+577점**. 이 중 도메인적으로 의미 있는 도약은 굵게 표시한 8개 실험이다.

기각·회귀한 제출은 §3에 별도로 모았다 (실전에서만 6번, 로컬에서 수십 번 꺾였다).

---

## 2. 점수를 올린 실험들 — 무엇을, 어떻게, 야구적으로 무슨 뜻인가

### 2.1 파생 피처 12개 + CatBoost 도입 (736.61 → 878.73)

**실험.** `asof_pitcher_*` / `asof_batter_*` 누적 지표를 조합: 최근 1/3/5경기 성공률 gap, 리그 평균 대비 상대 실력, 카운트 유불리, 풀카운트 여부, 압박 상황 교호작용. Optuna 40 trials로 CatBoost 튜닝.

**결과.** 파생 피처 +29.4, 튜닝 +52.5.

**도메인 의미.** 제구력을 예측하는 1차 신호는 **"절대 실력"이 아니라 "리그 대비 상대값"과 "카운트 국면"** 이다. 같은 60% 성공률이라도 리그 평균이 62%면 평균 이하 투수다. 3-0과 0-2에서 요구되는 제구는 질적으로 다르다.

### 2.2 CatBoost + MLP 블렌드 (→ 실전 924)

**실험.** 트리 계열(CatBoost)과 신경망 계열(MLP)의 예측을 `alpha·cat + (1−alpha)·mlp`로 그리드 서치. 두 모델 오차 상관계수 **0.9991**.

**결과.** 상관이 극단적으로 높은데도 alpha≈0.55~0.60에서 매끄러운 단봉 최고점, 로컬 +3점 / 실전 +72점(로컬 대비).

**의미(방법론).** 이질성은 "다른 알고리즘"이 아니라 **"실제로 다르게 틀리는가"** 에서 나온다. CatBoost는 축 정렬 분기로, MLP는 매끄러운 함수로 같은 데이터를 다르게 근사하기 때문에 남은 오차의 방향이 미묘하게 어긋난다. (나중에 TabM·3rd 모델이 실패한 이유가 정확히 이 기준을 못 넘겨서다.)

### 2.3 MLP 7-seed 앙상블 (로컬 690 → 790, 실전 첫 역전)

**실험.** 단일 MLP는 같은 설정에서도 epoch마다 Val Score가 490~690으로 요동. lr/weight_decay 조정은 무의미. 서로 다른 시드 7개를 독립 학습 후 평균.

**결과.** 로컬 690 → 790. 로컬은 CatBoost(818) > MLP(790)였으나 **실전은 MLP가 +10 우위**.

**의미.** 신경망의 병목이 하이퍼파라미터가 아니라 **분산**일 때는 앙상블이 답이다. 그리고 이 대회의 원형 교훈이 여기서 나왔다 — **로컬 순위는 실전 순위를 보장하지 않으며, 이 프로젝트에서 실전은 항상 로컬보다 높았다.** 15-seed·네트워크 확장은 무이득 → 7-seed로 고정.

### 2.4 Quantile(PLE) 수치 임베딩 (→ 실전 957.80) — *나중에 최종 기각됨*

**실험.** 수치형 컬럼을 표준화 후 concat하던 것을, 피처별 24분위 bin + piecewise-linear 인코딩 + 독립 Linear로 임베딩(Gorishniy et al.). baseline도 매번 같은 7-seed로 재학습해 paired 비교.

**결과(당시).** n_bins=24가 7/7 시드 승, +55.78. 실전 957.80.

**의미(당시).** 제구 관련 연속변수(성공률, win expectancy, li 등)는 선형이 아니라 **구간별로 효과가 꺾인다** — 성공률 0.55→0.60의 의미와 0.70→0.75의 의미가 다르다.

**반전(2026-08-28, §3.5 참고).** 유담 레시피로 갈아탄 뒤 PLE를 다시 얹어 제출 → 1077.25, raw 대비 **−15.75**. quantile bin이 학습 분포에 과적합되어 2025 레짐 시프트에서 외삽 실패. **최종 기각, raw 표준화로 복귀.** 로컬 cutoff7 +57은 노이즈였고, season2023 −15가 진짜 신호였다.

### 2.5 F1 필터 — 퓨처스리그 오염 데이터 제거 (→ 실전 971.41)

**실험.** `game_type` F(퓨처스/2군) vs R(1군)의 성공률을 시즌별로 분해했더니, 2022년까지 F가 R보다 최대 **+20.5%p 높다가 2023년부터 −3.0%p로 역전**. `game_type`은 CatBoost 피처 중요도 1위. 조치: `game_type=='F' & season<=2022` 행을 **학습에서만** 제거, 검증/추론은 원본 유지.

**결과.** season==2024 단독으로는 −19처럼 보이지만, season==2023 홀드아웃에서 unfiltered 모델은 **거의 0점으로 붕괴**(72 → 550, +478). 2023+2024 평균 +229.

**도메인 의미 — 이 프로젝트에서 가장 중요한 도메인 사실.** KBO는 **ABS(자동 볼-스트라이크 시스템)** 를 2020~2023 퓨처스리그에서 시범 운영하다 **2024년 1군 전면 도입**했다. "스트라이크존 한복판/이탈"로 정의되는 `control_success`는 **심판 존 → 기계 존**으로 판정 주체가 바뀌면서 정의 자체가 이동했다. 옛 심판 존(특히 2군)에서의 "제구 성공"은 새 기계 존의 그것과 다르다. **옛 레짐 데이터로 학습하면 새 레짐에서 무너진다.**

### 2.6 cutoff=7 split + 스태킹 메타모델 (→ 실전 982.22)

**실험.** "2024 전체 홀드아웃" → "2024년 3~6월은 학습, 7~10월만 홀드아웃". 블렌드는 고정 alpha → `sigmoid(w_cat·p_cat + w_mlp·p_mlp + b)` 로지스틱 스태킹(rolling-origin 3/3 fold 검증).

**결과.** 로컬 +60.85, 실전 +11.22. cutoff∈{4..10} × weight∈{1,5} 스윕에서 weight=1, cutoff=7이 최적.

**도메인 의미.** 규칙 변경(ABS) 직후에는 **"가장 최근 데이터"가 "가장 많은 데이터"보다 중요**하다. 2024 ABS 데이터를 조금이라도 학습에 넣어야 2025 ABS를 맞춘다. weight=5(2024 몰아주기)는 오히려 불안정 — 단일 시즌 가중은 여러 연도의 평균화·완충 효과만 깎는다.

### 2.7 시즌 진행분 피처 (→ 실전 1027.54, +45.32) — *프로젝트 역사상 가장 신뢰도 높은 피처 승리*

**실험.** 공식 `asof_pitcher_success_rate`는 **커리어 통산 누적 평균**이다. 이걸 두 조각으로 분해: (a) "이번 시즌만"의 성과(`_season_n`, `_season_success_count`, `_season_success_rate`), (b) 직전 시즌 마지막 행 기준선 대비 격차(`_rate_gap`). 투수/타자 각 4개, 총 8개. lookup은 train.csv 전체에서 사전 계산해 정적 CSV로 번들(다른 행 의존 금지 규칙 준수).

**결과.** CatBoost와 MLP **양쪽**, cutoff7과 season==2023 **양쪽 레짐** 모두 개선 — 이 조건을 전부 만족한 유일한 피처. cutoff7 blend +35.90 / season2023 +164.90.

**도메인 의미.** 제구력은 **"커리어 통산"이 아니라 "올 시즌 지금 얼마나 좋은가"**, 그리고 **"작년 대비 얼마나 변했는가"** 가 신호다. 선수는 시즌 단위로 폼이 바뀐다 — 부상, 구질 추가, 투구폼 수정, 노쇠화. 통산 3할 타자가 올해 2할5푼이면 통산값은 거짓말을 하고 있다.

### 2.8 Track A TE-residual (→ 실전 1041.40, +13.86)

**실험.** 투수/타자의 **상황별**(카운트·주자 축) target-encoding 잔차 = `관측 TE − prior`. `causal_smoothed_te_encode(source_df=train_split)`로 계산, val/test엔 정적 스냅샷. CatBoost 전용(MLP엔 악영향).

**결과.** 실전 +13.86. 이후 페어 매치업(§3.7)이 이 패턴을 잘못 베껴 val-leakage 버그를 냈다.

**도메인 의미.** 특정 국면(만루, 2스트라이크, 동점 9회)에서 **그 선수가 리그 기대치보다 얼마나 더/덜 제구하는가** = 상황별 제구 성향(clutch/choke). 평균 제구력과 별개의 축이다.

### 2.9 카운트별 구종 비중 (coarse pitch-mix)

**실험.** `trackman_history.csv`에서 `(balls, strikes, pitcher_hand, batter_hand)` 별 fastball/breaking/offspeed 사용 비율. **선수 정체성·시즌 조인 없음** → 구조적으로 리크 불가. CatBoost 전용.

**결과.** 두 레짐 모두 클린 통과(2023 +9.57 / cutoff7 +34.43). 트랙맨 파생 중 유일한 생존자.

**도메인 의미.** 볼카운트가 **어떤 구종을 던지게 강제하는지**가 제구 난이도를 대변한다. 3-0에서는 패스트볼 가운데 승부, 0-2에서는 유인 브레이킹볼 — 구종마다 제구 편차가 다르고, 카운트가 그 구종 분포를 결정한다. "누가 던지냐"가 아니라 "이 카운트가 뭘 요구하냐"라서 안전하다.

### 2.10 유담 레시피 전면 포팅 = candidate B raw (→ 실전 1092.998, +52)

**실험.** 팀원(조유담)의 검증 스크립트를 그대로 돌려 그의 수치(로컬 blend 815)를 재현 확인한 뒤, 파이프라인 전체를 그의 레시피로 교체: 트랙맨 상황조인(10-key) 복원 + `asof_pitcher_reverse_rate` 시즌 진행분 분해 + CatBoost v2 하이퍼파라미터(depth=7, lr=0.0468, l2=19.39) 5-seed + **raw 표준화 MLP**(PLE 제거) 7-seed.

**결과.** 실전 1092.998 — 유담 본인의 동등 제출(1092.55)과 **+0.45 = 노이즈 일치**. 포팅 충실성 확정(과거 1043→1085 재현 격차는 버그 있던 초기판이었음).

**도메인 의미.** `reverse_rate`(포수 요구 반대방향 투구율)의 시즌 진행분은 **제구 실패의 세 번째 정의(배터리 미스)를 직접 겨냥**한 피처다. §2.7 시즌 분해 트릭을 성공률이 아니라 "역방향율"에 적용한 것.

### 2.11 트랙맨64 상황조인 완전 제거 (→ 실전 1117.03, +24.03)

**실험.** 유담이 복원한 트랙맨 상황조인의 `match_cols`에 **`season`이 포함**되어 있다. 2025 추론 시 트랙맨(2019~2024)과 등호 매칭이 100% 실패 → 64개 물리량 파생 컬럼이 전부 **단일 상수로 붕괴**. collapse 시뮬레이션(val 트랙맨을 강제로 상수화)으로 측정: CatBoost solo가 상수 붕괴 하에서 **−174**로 miscalibrate. 64컬럼을 두 모델 피처 리스트에서 제거(CatBoost 143→79, MLP num 132→68).

**결과.** collapse-sim 4레짐 평균 +17.28 (레짐 반전 없음 — 여러 세션 만에 가장 깨끗한 신호). 실전 +24.03으로 **로컬이 오히려 저평가**(이 프로젝트에서 드문 방향).

**도메인 의미.** 트랙맨 30개 컬럼은 전부 **릴리스 시점 물리량**(구속, 회전수, 유도 수직/수평 무브먼트, 익스텐션, 릴리스 높이·좌우)이다. `control_success`는 **공의 최종 위치 vs 존 + 포수 타겟**으로 정의된다 — 트랙맨에는 위치 필드도, 포수 타겟 필드도 없다. **구조적으로 다른 축**이다. 게다가 죽은 상수 컬럼은 트리 모델의 분기 후보를 오염시켜 능동적으로 해를 끼친다.

### 2.12 cat_team — team_id를 CatBoost categorical로 (→ 실전 1126.77, +9.74) — *현재 확정 최고*

**실험.** `pitcher_team_id` / `batter_team_id`는 raw 데이터에서 int64라 CatBoost의 pandas auto-detect가 categorical로 못 잡는다. `.astype(str)` 캐스팅 + `cat_features=[...]` 명시 선언.

**결과.** blend Δ cutoff7 +5.53 / season2023 +2.47 (두 현실 레짐 모두 +). CatBoost solo Δ가 데이터 양에 **단조** — +21.87@126만행 → −101.86@43만행. 즉 소량 데이터 fold의 음수는 타깃 통계 희박성 아티팩트이고, 137만행 프로덕션 재학습에는 없다. 실전 +9.74로 로컬(+5.53) 저평가.

**도메인 의미.** **팀 단위 제구 경향** — 투수코치 성향, 배터리(포수) 리드 패턴, 홈구장 심판/ABS 존 특성, 팀 피칭 철학. 카디널리티 ~10, 레벨당 ~13만 행이라 CatBoost의 ordered target statistics가 이상적으로 작동한다. 반면 개인 `pitcher_id`/`batter_id`(~800종)를 categorical로 넣으면 표본 희박으로 solo −171 → **팀에는 되고 개인에는 안 된다.**

---

## 3. 점수를 내린 실험들 — 무엇이, 왜 실패했나

### 3.1 정체성(ID) 그 자체를 넣으려는 모든 시도

| 실험 | 결과 |
|---|---|
| `pitcher_id`/`batter_id` MLP 임베딩 | 로컬 660 → **304** (3 epoch 만에 과적합) |
| `season` MLP 임베딩 | 660 → **351** (검증셋에 미학습 카테고리) |
| ID를 CatBoost categorical로 | CatBoost solo **−171** |
| 정규화된 ID 임베딩 재시도 (10× weight_decay + dropout) | 여전히 solo −29 / blend −6.35 |
| `mlp_no_pid` (pid를 MLP num_cols에서 제거) | 실전 **1102.76**, −24.02 |

**도메인 의미.** **선수 정체성 자체는 신호가 아니다.** 그 선수의 정보는 이미 `asof_*` 누적 성적에 녹아있고, 원본 ID를 주면 모델은 암기만 한다. 게다가 선수 구성은 시즌마다 바뀌므로(FA, 은퇴, 신인) target-statistic도 견고하지 않다. 제구력은 **"누구인가"가 아니라 "지금 어떤 상태인가"**. (팀 ID가 §2.12에서 통한 건 팀은 표본이 충분하고 구성이 안정적이기 때문 — 정반대 조건.)

### 3.2 트랙맨 물리량 (릴리스 시점 측정값) — 4년에 걸친 반복 기각

| 시도 | 결과 |
|---|---|
| tier A/B/C/F (투수 ID 크로스워크 × 구종군 물리량 mean/std) | 실전 869.52, 950.81 회귀 |
| tier B 단독 | blend는 +처럼 보였으나 CatBoost solo **−55.72** (메타 재가중 착시) |
| 크로스워크 정교화 (568 → 695명) | 결론 불변 — 병목은 크로스워크가 아님 |
| row-level pseudo-label (86.73% 커버리지) | Cohen's d ~0.133 구조적, 7-seed 재검증서 사망 |
| richer aggregation (skew/분위수 192컬럼), recency (128컬럼) | 두 레짐 모두 노이즈 폭(±5~11) |

**도메인 의미 — 트랙맨 실패의 구조적 근거.** `control_success`의 정의는 **최종 공 위치(존 대비) + 포수 요구 방향**이다. 트랙맨 30개 컬럼에는 **위치 필드가 없고 포수 타겟 필드도 없다** — 전부 릴리스 시점 물리(구속/회전/무브/익스텐션/릴리스 포인트). **강속구 투수 ≠ 제구 좋은 투수.** 이건 "데이터가 부족해서"가 아니라 **"그 피처 공간에 담긴 정보량이 원래 거기까지"** 라는 정보 천장이다 — 4가지 독립적 방법으로 확인됐다. 정보 천장은 증강으로도, 더 무거운 아키텍처로도 못 뚫는다.

### 3.3 세 번째 모델 계열 (3-way 스태킹) — 장기 보류 → 최종 폐기

LightGBM/XGBoost/RandomForest(§11) → FT-Transformer/ExcelFormer(§22) → **DeepFM/EBM/BART/NAM**(2026-08-21). 마지막 4종은 CatBoost/MLP와 학습 메커니즘이 근본적으로 다르게(인수분해/cyclic bagging/베이지안 MCMC/피처별 shape function) 골랐는데도:

- DeepFM 3-way delta가 **동일 설정 재실행만으로 +1.87 → −7.26 부호 반전** (원 "개선"이 노이즈였음을 직접 증명)
- season-level rolling-origin(2020~2024): 4종 전부 mean delta << fold std
- 실전 제출 1041.83 (+0.43 = 노이즈)

**의미.** CatBoost + MLP 두 모델이 이미 **정보를 다 뽑았다.** solo↔correlation 트레이드오프가 정보 천장을 구조적으로 증명한다 — 상관을 낮추면 solo가 약해지고, solo를 튜닝하면 상관이 올라간다. "메커니즘이 달라도 solo가 프로덕션의 28~61%면 스태킹 이득 없음"이 정량 기준으로 추가됐다.

### 3.4 MLP 용량·시드 증가 — 딥러닝 직관이 안 통함

| 실험 | 결과 |
|---|---|
| 7 → 15 seed | 실전 **1034.94**, −6.46 |
| 7 → 20 seed | 로컬 KEEP_REF (개선 없음) |
| depth 3/4층 (plain), ResNet 2/4/6블록 | 블록 수 늘수록 단조 악화 |
| 폭 256/128 | 무개선 |
| flat-25 epoch 정책 (A10) | 실전 1119, −7.77 |

**의미.** 이 파이프라인에서 MLP의 역할은 **"CatBoost와 다르게 틀려서 블렌드에 다양성을 더하는 것"** 이지 자체 표현력을 키우는 게 아니다. **분산 문제(앙상블로 해결)와 정보 천장 문제(아무것도 해결 못함)는 다른 병**이고, 용량 확장은 후자를 못 고친다.

### 3.5 메타/블렌드 레이어 개조 — 전부 로컬만 좋고 실전 손해

| 실험 | 로컬 | 실전 |
|---|---|---|
| M4: 메타를 log-loss 대신 **Brier 직접 최소화** | 4/4 fold 양수 (+2.4~2.7) | **1103.80, −13.24** |
| context별 블렌드 가중치 (1축 로지스틱 상호작용, 8축) | 최대 +3.94 | regime-flip, 전부 기각 |
| post-blend calibration (isotonic/Platt/beta/상수 shift) | — | 전부 기각 |
| 수동 가중치 0.15/0.85, 0.45/0.55 | — | 1116.89, 1106.91 |

**핵심 규명 — 팀원의 "보정상수 +9"는 왜 우리에겐 없나.** 팀원은 **intercept 없는 고정 가중평균**(`0.61·cat + 0.39·mlp`)을 써서 base 모델의 ~2% 과소예측 편향이 그대로 남았고, r≈0.5 근처에서 BSS는 ~1.5% 편향 보정을 ~+9점으로 증폭한다. 우리의 **2-input 로지스틱 메타는 intercept가 이미 그 일을 한다** (held-out fold에서 shift b*≈0.0001, Platt slope≈1.000). 남길 게 없다.

### 3.6 피처 프루닝 — 로컬로도 실전으로도 사망 확정

| 실험 | 결과 |
|---|---|
| Cohen's d / null-importance 프루닝 (blk3) | rolling −18.75, wins 1/4 |
| **정확한 대수적 중복** 제거 (`run_total_before` = top+bot 등) MLP서 | 전부 2/4 승, MLP-solo 붕괴 (2021 fold −113~−294) |
| engineered 11-col 블록 제거 | 3-seed에서 노이즈로 수렴 |
| **G_interact catonly 제출** | 실전 **1077.23, −39.80** |
| reverse_rate 제거 | cutoff7 +18.77 이지만 2021 fold −82.64 (regime-flip) |

**의미 — 트랙맨64가 유일한 예외였던 이유.** 트랙맨64 제거가 통한 건 **"2025 추론에서 상수로 붕괴한다"는 구조적 메커니즘**이 있어서다. 그게 없으면 cutoff7 델타는 프루닝 근거가 못 된다. MLP 첫 레이어는 선형이라 정확한 선형 결합을 빼는 건 표현상 no-op인데도 solo가 ±30 요동친다 — **노이즈 바닥이 효과보다 크다.** 블록 하나 빼면 실전 −40점.

### 3.7 새 산술/재조합 피처 — "재표현·제거·레짐정렬"만 통하고 순수 신규는 전멸

| 실험 | 결과 |
|---|---|
| ratio/division 13종 (`rate A / rate B`) | Phase 1 wipeout (best −0.01, worst −19.84) |
| G1~G4 (failure-mode 직접 상호작용, 타자 대칭항, 상황축 TE, ABS-anchor 재정렬) | 19개 config 전멸 |
| career-trajectory / career-acceleration | dual-regime 부호 반전 |
| month-level trend / sin·cos 캘린더 | 세분화할수록 역효과 (−20.71/+27.87) |
| **pair-matchup** (투수×타자 as-of) | val-leakage 버그, 실전 **1028.75, −12.65** |
| team-matchup (재설계) | regime flip, mean −7.23 |

**의미.** **이 프로젝트에서 통한 8개 변화는 전부 재표현(시즌 분해, PLE 초기 채택) / 제거(트랙맨, F1 필터) / 레짐 정렬(cutoff7) 또는 이미 검증된 축의 확장(TE-residual, coarse pitchmix, cat_team)** 이다. **순수하게 새로운 외부/산술 피처를 더한 시도는 10개 넘게 전부 실패했다.** 제구력의 신호는 이미 공식 `asof_*` 컬럼에 대부분 들어있고, 재조합은 한쪽 solo만 올린 뒤 2-input 로지스틱 메타를 불안정하게 만들어 blend를 떨어뜨린다.

### 3.8 구조적 자작 후보 (2026-09-01 마감 세션) — 5개 전부 스크린 기각

| 실험 | cutoff7 | 진단 |
|---|---|---|
| D1: career rate를 CatBoost서만 제거 | −6.43 | CatBoost solo Δ **0.00** = 시즌 진행분과 완전 중복 |
| B: `monotone_constraints` 14피처 (성공률+1, middle/reverse−1) | −8.93 | CatBoost solo **−55.79** = ABS 레짐 구조 파괴 (rate↔success 관계가 비단조) |
| D2: cold-start 플래그 (`asof_n < 200`) | −3.51 | MLP 약화 (플래그류의 고질) |
| `mlp_drop_season` (season을 MLP서만 제거) | −4.98 / 실전 **1104.96 −21.81** | "2025 외삽 편향" 가설 **반증** — season은 진짜 신호 |
| `cat_drop_season` (양쪽 제거) | −3.28 | CatBoost solo −10.70 |

**의미.** `season`은 MLP에도 CatBoost에도 **진짜 load-bearing 신호**다(ABS 레짐, 연도 효과). "미래에 새 값이 나오는 컬럼"이라 임베딩은 금물이지만 숫자로는 반드시 있어야 한다. 그리고 다시 확인 — **구조적 2025-추론 실패 메커니즘이 없는 피처 추가/프루닝/정규화는 이 문제에서 통하지 않는다.**

### 3.9 F1 필터 OFF / 2024 가중치=5

- **F1 OFF**: season==2023 홀드아웃에서 `best_iteration=0~4`로 학습이 첫 트리에서 멈추며 점수 ~10~22로 붕괴. 팀원의 실전 F1-off 제출 965.84 (베이스라인 이하).
- **weight=5** (2024 몰아주기): cutoff 스윕에서 불안정, 2023 October 재확인서 부호 반전.

**의미.** 옛 레짐(pre-ABS) 데이터는 새 레짐 학습에 **독**이지만, **완전 제거도 안 된다**(표본 손실 −105점). 경계 = 2022가 최적. "얼마나 버릴 것인가"에 스위트 스팟이 있다.

### 3.10 증강 / 멀티태스크

- mixup / gaussian noise / label-smoothing / cutmix 4종: 전부 기각
- auxiliary multi-task (`middle_rate`/`ball_rate` 동시 예측): **−65 ~ −141**
- game_type 라우팅 (F/R 별도 헤드): 기각

**의미.** 제구 예측은 **표본이 부족한 문제가 아니다**(126만 행, 과적합 징후 없음, CatBoost 정상 조기종료). 증강은 분산만 고치는데 병목은 분산이 아니다. 멀티태스크로 실패 유형(한복판/볼)을 동시에 맞히게 하면 오히려 주 타깃 학습을 방해한다 — 세 실패 유형은 서로 다른 메커니즘이라 공유 표현이 손해다.

---

## 4. 핵심 방법론 교훈 (이 도메인에서 얻은 것)

1. **로컬 홀드아웃 < 실전 리더보드, 항상.** 이 프로젝트에서 실전이 로컬보다 낮았던 적은 초반 몇 번을 빼면 없다. 로컬·실전 충돌 시 실전을 신뢰한다.
2. **"cutoff7 소폭 양수"는 노이즈다.** M4 메타(−13.24), PLE(−15.75), G_interact(−39.80), nopidmlp(−24.02), 15-seed(−6.46), mlp_drop_season(−21.81) — cutoff7만 +였던 6개 제출이 전부 실전에서 꺾였다.
3. **single-seed → multi-seed에서 부호가 반복적으로 뒤집힌다.** 3-seed 스크리닝은 "탈락 후보를 빠르게 거르는 용도"로만. 통과해도 rolling-origin 없이 채택 안 함.
4. **cutoff7 ↔ season2023 regime-flip은 진단됐다.** (a) F1 필터가 season2023 train에서 F리그를 100% 제거하는데 val엔 10.46% 섞임, (b) cutoff7 val은 R리그 train→val 성공률 이동이 −3.32%p로 season2023(−1.99%p)보다 더 어려운 일반화 테스트(ABS 시프트 끝단). cutoff7 우선 관행은 경험칙이 아니라 구조적으로 정당하다.
5. **구조적 메커니즘이 있는 변화만 통했다.** 트랙맨64 제거(2025 상수 붕괴), F1 필터(F/R 관계 역전), cutoff7(레짐 정렬), 시즌 분해(폼은 시즌 단위), cat_team(팀은 표본 충분). 메커니즘 없는 델타는 프로덕션 근거가 아니다.
6. **미래에 새 값이 나오는 컬럼(`season`)은 임베딩 금지, 숫자로 유지.** MLP 임베딩·CatBoost target-statistic 양쪽에서 재현된, 모델 종류를 안 가리는 교훈.
7. **이질성은 "다른 알고리즘"이 아니라 "실제로 다르게 틀리는가"** 에서 나온다. TabM(신경망 계열)은 MLP와 상관이 비슷해 이득 없었다.
8. **메타모델 재가중이 서브모델 훼손을 가릴 수 있다** — blend 델타만 보지 말고 CatBoost/MLP solo 점수도 봐야 한다 (트랙맨 tier B 착시).
9. **검증 fold는 "답하려는 질문"과 경계가 일치해야 한다.** 방법론적으로 흠 없는 TimeSeriesSplit(8-fold)도 전부 같은 4개월 안이면 "이게 시즌을 넘어 일반화되나"에 답하지 못한다.
10. **자체 계산 row-level 누적 통계는 항상 TE-residual 패턴**(train_split만 source, val/test엔 정적 스냅샷)을 따라야 한다 — 페어 매치업이 시즌 분해 패턴을 잘못 베껴 val-leakage 버그를 냈다.

---

## 5. 최종 통찰 — '제구력'이란 이렇게 정의할 수 있다

577점의 실험이 수렴한 결론:

> ### 제구력(control ability)은 "구위"가 아니라 **"투수가 의도한 코스와 공의 실제 코스가 일치하는 정도"**이며, 그것을 예측하는 신호는 선수의 물리적 재능이 아니라 **[현재 폼] × [상황 적응] × [매치업 이해] × [규칙 환경]** 에 있다.

구체적으로, 제구 성공 확률은 다음 **다섯 축**의 함수다:

**01. 현재 폼 — 시즌 단위, 그리고 변화량.**
커리어 통산이 아니라 "올 시즌 지금 얼마나 좋은가", 그리고 "작년 말 대비 얼마나 변했는가". → 시즌 진행분 피처(+45)가 프로젝트 최대 피처 승리. 선수는 시즌 단위로 변한다(부상·구질·폼·나이).

**02. 상황 압박 — 카운트 · 주자 · 레버리지.**
볼카운트 국면(3-0 vs 0-2), 주자 상황, 경기 중요도(li). 그리고 절대 실력이 아니라 **"이 상황에서 이 선수가 리그 기대치 대비 얼마나"** (TE-residual, +14). 평균 제구와 clutch 제구는 다른 축.

**03. 매치업 맥락 — 구종 · 손 · 팀.**
카운트가 강제하는 구종 분포(coarse pitchmix), 좌우 매치업(same_hand), 그리고 **팀 단위 배터리·코칭·홈존 경향**(cat_team, +9.7). 개인 ID는 암기만 유발하는 노이즈지만, 팀은 표본이 충분한 신호.

**04. 규칙 환경 — 존을 누가 판정하는가.**
ABS(자동 볼-스트라이크) 도입으로 `control_success`의 **정의 자체가 이동**했다(심판 존 → 기계 존, 2024 1군 전면). 옛 레짐 데이터는 새 환경 예측에 독 → F1 필터(+14), cutoff7 split(+11). `season`은 반드시 유지해야 하는 진짜 신호.

**05. 정의상 배제되는 것 — 공의 물리.**
구속 · 회전수 · 무브먼트 · 익스텐션 · 릴리스 포인트(트랙맨 30컬럼)는 제구력과 **구조적으로 다른 축**이다. `control_success`는 "최종 위치 + 포수 요구 방향"으로 정의되고 릴리스 물리는 거기 없다. 4년간 모든 트랙맨 실험이 실패했고, 유일하게 통한 트랙맨 관련 변화는 트랙맨을 **제거**한 것(+24)이었다. **강속구 투수는 제구 좋은 투수가 아니다.**

---

### 한 문장 요약

**제구력은 그 투수가 "지금(시즌 폼)" "이 상황에서(카운트·주자)" "이 배터리·이 존 규칙 아래(팀·ABS)" 원하는 코스로 공을 넣을 확률이며, 얼마나 빠르고 강한 공을 던지는가와는 무관하다.**

---

*문서 생성: 2026-09-01. 근거: `PROJECT_HISTORY.md`, `EXPERIMENTS.md`, `CLAUDE.md`, 대시보드 제출 기록 100+건. 현재 프로덕션 = cat_team 번들, 실전 1126.77.*
