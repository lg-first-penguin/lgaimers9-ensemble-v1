# 프로젝트 히스토리 — 실험 전체 타임라인

`control_success`(투구 제구 성공 확률) 예측 파이프라인이 현재의 "트랙맨 미사용 + F1 필터 + cutoff=7(2024 head 포함) 분할 + 재튜닝 CatBoost + quantile-embedding MLP 스태킹 블렌드"(실제 대시보드 982.22점)까지 도달하기까지, 시도한 모든 EDA·피처 엔지니어링·모델 구성 변경을 **왜 시도했는지 → 무엇을 했는지 → 결과가 어땠는지 → 왜 채택/기각했는지** 순서로 정리한다.

세부 수치·코드 스니펫·rolling-origin fold별 원자료는 `EXPERIMENTS.md`(§ 번호로 상호 참조)를 참고. 이 문서는 그걸 관통하는 **의사결정의 흐름**을 읽기 위한 문서 — "지금 왜 이 구조인가"를 다시 파악해야 할 때는 이 문서를, 특정 수치의 근거나 재현 방법이 필요할 때는 `EXPERIMENTS.md`를 보면 됨.

## TL;DR — 점수 타임라인

| 단계 | 로컬(season==2024) | 실제 대시보드 | 비고 |
| --- | --- | --- | --- |
| RandomForest 베이스라인 (대회 제공 템플릿) | ~549.51 (수료 기준) | — | 시작점 |
| CatBoost + 파생 피처 12개 + Optuna 튜닝 | 818.54 | 878.7276733562 | EDA 1라운드 |
| Tabular MLP 단일 모델 (마이그레이션 직후) | 660.61 | — | Colab "1,500점" 추적 중 |
| Tabular MLP 7-seed 앙상블 | 789.58 | CatBoost보다 +10 우위 | 로컬·실전 순위 최초 역전 |
| CatBoost + MLP 알파 블렌드 (alpha≈0.59) | 851.19 | **924** | 최초 프로덕션 블렌드 |
| + 스태킹 메타모델(alpha 대체) | 855.31 | *(미제출)* | rolling-origin 3/3 fold 검증 |
| + quantile(PLE) 수치형 임베딩 | 879.34 | **957.80417** | 역대 최고 기록 |
| (버그) 트랙맨 64피처 실배포 100% 죽음 발견 | — | *(957.80417에 이미 반영된 상태로 숨어있었음)* | §16 |
| 트랙맨 완전 드롭 + F1 필터 | 777.61 | *(미제출)* | 로컬 수치는 낮아 보이지만 축 분리 검증으로 순효과 확인 |
| + CatBoost 재튜닝 | 794.77 | **971** | 현재 프로덕션, 역대 최고 |
| MLP 하이퍼파라미터 재탐색 (Optuna, 경계선) | 818.69 | 957.85 | §30 — 기각, 971로 복원 |
| CatBoost GPU 재탐색 + 프로덕션 GPU 전환 | 812.38 | 967.36 | §32 — 근소하게 하락(−3.64), 기각·971로 복원 |
| ABS 레짐 시프트(2024 head 포함, cutoff=7) | 705.34 (2024 7~10월 기준) | **982.22** | §34/§37 — 채택 확정, +11.22, 현재 프로덕션·역대 최고 |

로컬 숫자가 위아래로 출렁이는 것(879.34 → 777.61 → 794.77)에 속지 말 것 — 이 프로젝트의 핵심 교훈 중 하나가 "season==2024 단일 홀드아웃은 순위·크기 모두 신뢰할 수 없다"는 것이었다(아래 "핵심 교훈" 참고). CatBoost 재튜닝(971)까지는 실제 대시보드 점수가 매 순간 로컬보다 높게 나왔고 방향도 로컬과 어긋난 적이 없었지만, §30의 MLP 하이퍼파라미터 재탐색에서 처음으로 이 패턴이 깨졌다 — 로컬은 +23.92 개선(794.77→818.69)이었는데 실전은 −13.15 하락(971→957.85)이었다. 즉 "로컬이 오르면 실전도 오른다"는 경험칙은 절대 규칙이 아니라 지금까지의 변경들이 우연히 방향이 일치했던 것일 수 있다 — 경계선 로컬 결과(특히 시즌 간 부호가 갈리는 경우)는 실전 방향까지 보장하지 않는다.

---

## 1. 출발점 — RandomForest 베이스라인

대회가 제공한 베이스라인(`RandomForestClassifier`, depth=10, min_samples_leaf=200, season==2024 홀드아웃)에서 시작. 수료 기준(리더보드 549.51)은 이 베이스라인을 그대로 돌렸을 때의 점수다.

## 2. CatBoost 도입 — 피처 엔지니어링 + Optuna 튜닝 (736.61 → 818.54)

RandomForest에서 CatBoost로 전환한 초기 과정 자체는 별도 기록이 남아있지 않지만(`EXPERIMENTS.md`가 시작되는 시점의 reference가 이미 BSS 736.61), 이 시점부터 본격적인 실험 로그가 시작된다.

**피처 엔지니어링** — `asof_pitcher_*`/`asof_batter_*` 누적 지표를 조합한 파생 피처 12개 추가(`add_engineered_features`): 최근 1/3/5경기 성공률 gap, 리그 평균 대비 상대 실력, 카운트 유불리, 풀카운트 여부, 압박 상황 교호작용 등. `league_success_mean`은 "직전 시즌 평균"으로 정교화하려던 원안을 복잡도 대비 이득이 적다고 판단해 **전체 학습 구간 단일 스칼라**로 단순화. → **+29.4점 (736.61→765.99)**.

**하이퍼파라미터 튜닝** — Optuna(TPE, 40 trials)로 `depth`/`learning_rate`/`l2_leaf_reg`/`random_strength`/`bagging_temperature`/`border_count`/`min_data_in_leaf` 탐색. → **+52.5점 (765.99→818.54)**.

이 시점의 CatBoost가 이후 한동안 "넘어야 할 기준선" 역할을 한다.

## 3. 모델 전면 교체 — CatBoost → Tabular MLP

**계기**: 사용자가 Colab에서 훨씬 단순한 피처셋(트랙맨·파생 피처 없이 원본 47컬럼, 범주형 3개만 임베딩)의 PyTorch MLP로 **1,500점**이라고 보고. CatBoost를 MLP로 전면 교체했다(트랙맨 병합·파생 피처 파이프라인은 유지, 모델만 교체).

**실제 데이터로 첫 실행 — 660.61, Colab과 큰 괴리.** 원인을 세 갈래로 추적:
1. 트랙맨 제외 비교(626.66) — 트랙맨이 원인이 아님을 배제.
2. 트랙맨 병합의 "시간 필터"가 실제로는 아무 행도 제거하지 않음을 확인 — 그래도 `match_cols`에 `season`이 있어 구조적으로 리크는 불가능, 배제.
3. **Colab 코드 재검토(사용자 확인) — 진짜 원인 발견.** `if val_data.shape[0]==0:` 폴백이 트리거되어, 의도한 `season==2024` 시간 기준 홀드아웃이 아니라 **무작위 20% 분할**로 검증되고 있었다. 무작위 분할 + `pitcher_id`/`batter_id`를 원본 숫자로 그대로 넣는 조합은 동일 선수의 투구가 train/val에 섞여 들어가는 **선수-정체성 리크**를 유발한다.

**→ Colab의 1,500점은 무효한 벤치마크였다.** 목표를 "1,500점 재현"에서 "리크 없는 season==2024 홀드아웃 기준으로 CatBoost(818.54)를 따라잡기"로 재설정.

### 3.1 MLP 자체 개선 시도들

- **범주형 컬럼 확장(3개→10개, 실패, 660.61→303.79 급락)**: `pitcher_id`/`batter_id`(고카디널리티, ~1.2M 학습행 대비 792/830종 → 소수 표본 암기)와 `season`(검증셋 2024가 학습셋에 아예 없는 값 → 미학습 카테고리 랜덤 임베딩 공유 → 체계적 miscalibration)이 원인. `season`은 **미래에 반드시 새 값이 나오는 컬럼은 임베딩이 아니라 숫자로 다뤄야 외삽 가능**하다는 일반 교훈으로 이어짐(실제 평가 test.csv도 2025로 train.csv에 없는 값). `pitcher_id`/`batter_id`/`season` 셋 다 제외(`CAT_COLS` 7개로 정착) → **690.62**로 회복.
- **하이퍼파라미터 스윕(유의미한 개선 없음)**: lr/weight_decay 조정 무의미. 대신 **같은 설정에서도 epoch마다 Val Score가 490~690점대로 요동**치는 걸 발견 — 병목이 하이퍼파라미터가 아니라 **분산 자체**임을 확인, 방향 전환.
- **시드 앙상블(Deep Ensemble) — 채택**: 서로 다른 시드 7개를 완전 독립 학습 후 평균. → **789.58**. 15-seed(768.93, 정체)·네트워크 확장(777.92, 무개선) 모두 추가 이득 없어 **7-seed, 128/64 네트워크**로 확정.

MLP 최종(789.58)은 CatBoost(818.54)를 아직 못 넘김 — 하지만 이후 §4에서 이 격차가 로컬에서만 존재하는 착시였음이 드러난다.

## 4. 로컬 vs 실전 첫 역전 — "로컬을 과신하지 말라"는 이 프로젝트의 원형 교훈

MLP 단독 버전을 먼저 실제 제출. **로컬은 CatBoost(818.54) > MLP(789.58)였는데, 실제 대시보드에서는 MLP가 CatBoost보다 +10점 높게 나왔다.** 추정 원인: CatBoost의 Optuna 튜닝이 `season==2024`라는 특정 홀드아웃에 최적화되며 그 시즌 고유 패턴에 살짝 과적합됐을 가능성, 반면 MLP는 시드 평균으로 분산을 낮춘 덕에 완전히 새로운 시즌(2025)에 오히려 더 잘 일반화됐을 가능성.

**이후 방침**: 로컬과 실전이 상충하면 실전을 신뢰한다. 이 패턴은 이후 모든 제출에서 예외 없이 재현된다(§6.3, §15.5, §22 — 실전이 로컬보다 낮았던 적은 한 번도 없음).

## 5. TabM(BatchEnsemble) 1차 시도 — 보류

Deep Ensemble(7배 학습 비용)의 저비용 대안으로 BatchEnsemble/TabM 스타일(공유 백본 + 멤버별 rank-1 승수 벡터) 자체 구현. r/s를 N(1,0.1)로 초기화 → 607.82(단일 MLP보다도 낮음, epoch=7 조기종료로 과적합 양상). weight decay에서 r/s 제외해도 597.32로 무변화. **잔여 가설**: 초기화가 항등에 가까워 멤버 분화가 느렸을 가능성(원 논문의 Rademacher(±1) 초기화 필요) — 검증하지 않은 채 보류(§11에서 재도전).

## 6. CatBoost + MLP 블렌딩 — 최초 프로덕션 채택, 실제 924점

**아이디어**: 트리 계열(CatBoost)과 신경망 계열(MLP)은 오차 패턴이 다를 테니 블렌딩하면 이득이 있지 않을까. 이미 학습된 두 모델의 예측을 `alpha*CatBoost + (1-alpha)*MLP`로 그리드 서치(재학습 불필요, 수 분 내 검증). **alpha≈0.55~0.60에서 매끄러운 단봉 최고점, 854.09** — 두 모델 오차 상관계수가 0.9991로 극히 높았음에도 유의미한 개선.

파이프라인 정식 반영(`code/catboost_model.py`/`code/blend_model.py` 신규, 번들 스키마에 `alpha` 추가) 후 완전 재학습으로 재검증: **로컬 851.19**. **실제 대시보드 924점** — 로컬 대비 +72.81, MLP 단독 때(+10)보다 격차가 더 벌어짐(§4 패턴 재확인, 오히려 심화).

## 7. CatBoost 카테고리 확장 시도 — 실패

두 모델 상관관계가 0.9991로 너무 높다는 문제의식에서, CatBoost가 target-statistic 기반이라 MLP 임베딩보다 고카디널리티 ID를 잘 다룰 것이라 가정하고 `pitcher_id`/`batter_id`를 CatBoost 카테고리로 선언. → CatBoost 단독 818.54→**647.20**(-171.3), 블렌드도 851.19→**788.12**(-63.1, alpha가 MLP 쪽으로 크게 이동). §3.1의 MLP 임베딩 실패와 동일 원인 — target-statistic도 시즌마다 선수 구성이 바뀌는 시프트 앞에서는 견고하지 않음. **결론: 모델 종류와 무관하게, 시즌마다 값이 바뀌는 식별자성 컬럼은 카테고리로 선언하지 말 것.**

## 8. 스태킹 메타모델 — 고정 alpha를 대체, rolling-origin으로 안정성 검증 후 채택

alpha 고정 가중평균 대신 `[cat_pred, mlp_pred]` → `LogisticRegression` 스태킹이 나은지 검증. 단일 시간 분할(9~10월 val)에서 **+8.0점** 우위 확인 후, 우연이 아님을 확인하기 위해 **rolling-origin 3-fold**(5~6월/7~8월/9~10월을 순서대로 val)로 재검증 — **3/3 fold 전부 스태킹 승리, 평균 +14.22점**. fold별 최적 alpha가 0.64~0.86으로 시점마다 다르다는 것도 확인 — 고정 alpha로는 못 잡는 시점 의존성을 스태킹이 일부 흡수하는 것으로 해석. **채택**, 정식 반영 후 재검증 855.31.

상황 피처(카운트/이닝/주자 등)를 메타모델에 추가하는 것도 시도했으나 역효과(-3.0) — CatBoost/MLP가 이미 그 정보를 직접 학습해서 `cat_pred`/`mlp_pred`에 녹아있으므로 메타모델이 얻을 잔여 신호가 없었고, 오히려 무의미한 피처가 핵심 두 피처의 계수를 L2로 희석시킴.

## 9. MLP 앙상블 부트스트랩 시도 — 실패

블렌딩 이질성을 더 키우려고, 7-seed 각각을 전체 데이터 공유 대신 복원추출(N은 유지)로 바꿔봄. 상관관계는 소폭 감소(0.894→0.874)했지만 MLP 단독 성능이 -52.75로 크게 떨어져 스태킹도 손해(-7.6). 복원추출이 N은 유지해도 유니크 행 기준 평균 63.2%만 노출되는 효과라 사실상 서브샘플링과 다르지 않았음. **기각.**

## 10. TabM 재도전 — Rademacher(±1) 초기화, 가설은 맞았지만 채택 미달

§5의 잔여 가설(초기화 문제)을 원 논문 방식(Rademacher ±1)으로 검증. **607.82(N(1,0.1)) → 708.39(Rademacher, +100.57)** — 가설 확인. 다만 여전히 Deep Ensemble(777~790)에는 못 미치고, CatBoost와 스태킹해도(832.83) 현재 프로덕션 스태킹(855~861)보다 낮음. **결론: 가설은 검증됐지만 채택 기준 미달, 종결.**

## 11. 세 번째 모델 계열 추가 — LightGBM/XGBoost/RandomForest 3-way 스태킹 (장기 보류 → 최종 폐기)

여기부터 여러 라운드에 걸쳐 검증되고, 이 세션에서 최종적으로 닫힌 긴 줄기다.

**1라운드(§12)**: `[cat_pred, mlp_pred, third_pred]` 3피처 스태킹. LightGBM(-1.03), **XGBoost 튜닝 없는 기본값(+7.36, 유일한 순이득)**, RandomForest(-6.36, 상관관계는 가장 낮았지만 단독 성능이 너무 약해 노이즈만 더함). rolling-origin 3-fold 재검증(§12.1)에서 XGBoost는 2/3 fold 승리·평균 +5.07 — §9의 채택 기준(3/3, +14.22)에 못 미쳐 **보류**(폐기 아님, 열어둠).

**2라운드(§12.2)**: 팀원이 별도로 튜닝한(Optuna 55 trials) 훨씬 강한 XGBoost(단독 744.38, 상관관계 0.934)를 대신 넣었더니 **오히려 더 나빠짐**(rolling-origin 1/3 승리, 평균 -7.55) — 단독 성능이 오르면서 CatBoost와의 상관관계도 같이 올라가 이질성 이득이 상쇄됨. "더 강한 모델을 넣으면 스태킹도 좋아질 것"이라는 직관이 틀린 사례.

**소표본 아티팩트 진단(§13.1)**: 튜닝 XGBoost가 5~6월 fold에서 보인 -28.37이 "그 시점이 나빠서"가 아니라 "그 fold의 메타모델 학습 표본이 3~4월치(5.5만행)뿐이라 계수(`w_mlp`)가 불안정해진 것"임을 확인 — 학습 표본을 3배로 늘리자 손해가 -28.37→-7.09로 대부분 해소. 실제 프로덕션은 메타모델을 25만행 전체로 학습하므로 이 아티팩트 자체가 재현되지 않음. (교훈: rolling-origin CV의 극단적 fold는 그 fold의 학습 표본 크기부터 의심할 것.)

**RandomForest 자체 개선(§13.2)**: RandomForest의 단독 성능이 낮은 이유가 배깅 자체의 한계가 아니라 **목적함수 불일치**(Gini는 Brier와 다름)임을 짚고, `RandomForestRegressor`로 0/1 타겟을 직접 회귀(MSE=Brier와 수학적으로 동일)시켜 **521.88→667.94(+28%)** 개선. 그래도 3-way 채택 기준(rolling-origin 평균 -2.75~-2.93)은 못 넘음.

**트랙맨 버그 발견 후 재검증(§16.2, 이번 세션)**: §16에서 트랙맨 매칭 버그(아래 §14 참고)를 발견한 뒤, "지금까지 모든 3-way 실험이 트랙맨이 실제로 매칭되는 조건에서 나온 거라면 결론이 바뀌지 않을까"라는 질문에 답하기 위해 (트랙맨 조건 as-is/fallback) × (레퍼런스 모델 구/신) 2축으로 재검증. **진짜 원인은 트랙맨이 아니라 레퍼런스 모델(quantile embedding 채택 후 2-way 베이스라인 자체가 강해짐)이었다** — XGBoost의 유일한 플러스(+7.36)가 트랙맨 조건은 그대로 두고 레퍼런스만 최신으로 바꾸자 즉시 마이너스(-2.91)로 뒤집힘. 트랙맨 조건(fallback) 축은 오히려 델타를 0에 가깝게 당겼을 뿐, 어느 쪽도 플러스로 전환시키지 못함. **최종 결론: "3번째 모델 계열 추가, 프로덕션 미채택" 판단 유지, 더 확실하게 닫힘.**

## 12. CatBoost/MLP 개별 성능 개선 5종 스크리닝 — 전부 실패

"새 모델 추가" 대신 "기존 두 모델을 각각 더 좋게" 방향으로 5개 후보를 CatBoost/MLP 단독 스크리닝:

- **트랙맨 매칭 지문(fingerprint) coarsening**: 현재 10-key 매칭이 지문당 매칭 건수 중앙값 1(선수 ID 직접 조인 불가능한 데이터 구조상 "가장 비슷한 상황 근사"라는 의도적 설계 가능성)이라는 관찰에서, "표본이 적어 보이니 성기게 하면 안정적이지 않을까"를 검증. `game_dayofweek`/`inning`/`game_month`를 순차 제거해 중앙값을 1→4→20→138까지 늘렸지만 **전부 큰 폭 손해**(-118.81~-158.57) — 세분화 자체가 신호였음. **현행 유지.**
- **`asof_batter_*` 강화**(7개 피처): -43.89. **pitch-mix × 상황 교차 피처**(7개): -39.06. 둘 다 feature importance는 소폭 잡혔지만 순효과 마이너스 — 일부는 기존 피처의 단조변환이라 새 정보가 거의 없고, 무의미한 피처가 늘며 CatBoost의 `random_strength` 무작위 분할 후보 풀을 희석시켰을 가능성(가설, 미확정).
- **loss function을 Brier(MSE)로 교체**: MLP(`BCELoss`→`MSELoss`)는 노이즈 이내로 무결론. **CatBoost(`Logloss`→`RMSE`)는 -35.97로 손해** — §13.2의 RandomForest와 정반대 결과. RandomForest는 그래디언트 없이 노드별 불순도만 보므로 "불순도 기준과 평가지표의 정합성"이 목적함수 선택의 전부였지만, CatBoost는 그래디언트 기반이라 cross-entropy(Logloss)가 squared error보다 0/1 근처에서 최적화 지형이 낫다 — 같은 트릭이 모델 계열에 따라 정반대로 작동한 사례.

5개 후보 전부 baseline을 못 넘어, 이 시점에서 "CatBoost/MLP 조합이 이미 성숙한 로컬 최적점 근방"이라는 잠정 결론.

## 13. 수치형 피처 임베딩 — periodic 실패, quantile(PLE) 채택 (n_bins=24)

CatBoost의 분기 표현력을 MLP가 따라잡기 위한 최신 기법(Gorishniy et al.) 시도. 지금까지 수치형 116개 컬럼은 표준화만 해서 concat하고 있었음.

**Periodic embedding**: `[sin(2πcx), cos(2πcx)]` 기반, sigma(주파수 스케일) 그리드 스윕. sigma=1.0은 명확히 붕괴(-387.40, 시드 하나 학습 실패) — 고주파가 최적화 지형을 깨뜨린다는 예상대로. 하지만 **작은 sigma 구간(0.001~0.1)은 델타가 4~28점으로 시드 간 자연 변동폭(std 23.69)에 묻히고, 인접 값끼리 부호가 뒤집히는 등 노이즈 패턴**. 가장 좋아 보인 sigma=0.001을 7-seed 전체로 재검증하니 +28.45→+4.15로 주저앉음(3-seed 스크리닝의 검정력 부족이 원인). **미채택.**

**Quantile(PLE) embedding**: 피처별 quantile bin + piecewise-linear 인코딩 + 독립 Linear. n_bins=8로 처음 시도했을 때도 델타가 baseline std 안에 묻히고 승률 3/7로 애매(잠정 노이즈 판정, 결론 보류). n_bins 그리드를 재개해 4/12/16/24/32를 스윕하고 **이번엔 baseline도 매번 같은 7-seed로 재학습해 승/패까지 paired 비교** — n_bins=16(+50.06, 7/7 승)과 24(+55.78, 7/7 승)가 델타 크기·승률·분산(오히려 baseline보다 안정) 세 지표 모두에서 뚜렷하게 우수, n_bins=12(5/7, 애매)와 32(6/7, 특정 시드에서 크게 패)는 그보다 못함. **n_bins=24, d=8 채택.**

전체 파이프라인(`dopip.py`)으로 재확인: 블렌드 855.31→**879.34**(+24.03), 새 reference로 승격. **실제 리더보드 제출 — 957.80417점**(로컬 대비 +78.5, 이전 924 대비 +33.8). §4 이후 반복된 "로컬이 실전을 과소평가" 패턴이 이번에도 재현, 채택 확정.

## 14. 트랙맨 매칭 오류 — 실배포에서 64피처가 상시 0으로 죽어있었음

957.80417점 제출 직후 발견. 트랙맨 병합의 `match_cols`(10개)에 `season`이 포함돼 있는데, **실제 평가 데이터(test.csv)는 항상 season==2025이고 트랙맨 데이터는 2019~2024까지만 있다** — season 등호 조건 때문에 실배포에서 매칭이 구조적으로 100% 실패. 5행 샘플 test.csv로 직접 재현(5/5 완전 미매칭 확인). 로컬 검증(season==2024)은 트랙맨 데이터가 2024까지 있어 우연히 이 문제를 가리고 있었다 — **지금까지의 모든 로컬 실험이 "트랙맨이 val 시즌을 볼 수 있는" 조건에서 측정된 것**이라는 뜻이기도 하다.

**1차 수정 — season fallback (추론 전용)**: season 등호 매칭 실패 시에만 season을 뺀 9-key로 재매칭. 학습 코드는 건드리지 않음(season 등호가 학습 시점의 유일한 시간 리크 방지 장치이므로). 로컬 시뮬레이션(트랙맨에서 season==2024를 지워 실배포 조건 재현): as-is(880.38, 참고용) → broken(730.25, 실배포 그대로) → **fallback(752.42, +22.17)**. 채택, 재학습 없이 `submit/script.py`만 수정.

**파생 질문 — 3-way 스태킹 결론도 재검증해야 하나?**: §11의 "트랙맨 버그 발견 후 재검증" 참고 — 재검증했고 결론 불변, 더 단단해짐.

## 15. 트랙맨 최종 결론 — ablation·asof 재설계 끝에 전부 드롭

fallback으로 일단 봉합한 뒤, "애초에 트랙맨을 안 썼다면?"을 검증.

**Ablation(트랙맨 완전 제거)**: fallback(752.42)보다 **+42.71 더 좋음**(795.13), broken(전부 0, 730.25)보다도 좋음. 원인: 학습-서빙 분포 불일치(skew) — 학습은 항상 season이 정확히 매칭되는 "날카로운" 분포를 학습하는데, 서빙은 항상 매칭 실패해 9-key로 재매칭된 "뭉뚱그려진" 분포를 받는다. "일부라도 매칭시켜주자"는 patch가 오히려 역효과였다.

**asof9key 재설계**: skew의 근본 원인이 학습/서빙 매칭 방식의 비대칭이라면, 학습도 서빙과 같은 방식(season 컷오프 + 9-key, 리크 없음)으로 맞추면 어떨까. season==2024·F1 미적용 기준 **804.04**(ablation 대비 +8.91) — 트랙맨 매칭 방식 중 최선.

**F1 필터 도입 후 2023 홀드아웃 재검증 — 결정적**: F1 필터(§16)를 채택하기로 하면서, season==2024 단일 홀드아웃의 신뢰성 문제(§16.1)를 감안해 asof9key도 F1+2023 홀드아웃으로 재확인. 같은 조건에서 asof9key가 no-trackman보다 **-29.11 더 나쁨** — 2024·F1=OFF에서 봤던 +8.91 우위가 정확히 뒤집힘. 2023+2024 평균은 사실상 0(-1.35) — **개별 시즌 결과는 노이즈였고, 트랙맨(어떤 매칭 방식이든)에 안정적인 신호는 없다.** 팀원이 독립적으로 검증한 3가지 트랙맨 접근(상황 지문 조인, 투수 ID 통산 물리량, 실시간 상태 — §16.3 참고)도 전부 실패로 정확히 수렴.

**최종 결론: 트랙맨 프로덕션에서 완전 드롭.** `process_trackman_features_safe` 및 관련 코드 전부 제거.

## 16. 팀원 제보 — 검증 방법론 맹점 + F1 필터

팀원이 독립적으로 진행한 검증에서 두 가지를 제보:

**16.1 검증 방법론 문제**: season==2024 단일 홀드아웃은 그 해의 리그 기준선 변동폭(r)에 점수가 지배당한다 — 동일 모델을 season==2023으로 재검증하면 완전히 붕괴(팀원 리포트 기준 0.0점). §15의 asof9key 재검증에서 정확히 이 패턴이 재현됐다(§15 마지막 문단).

**16.2 F/R 성공률 관계 역전**: `game_type` F(퓨처스)/R(1군) 성공률을 시즌별로 분해하면, 2022년까지 F가 R보다 최대 +20.5%p 높다가 **2023년부터 -3.0%p로 역전**되어 2024까지 유지됨(우리 데이터로 소수점까지 재현 확인). `game_type`은 CatBoost 피처 중요도 1위(§2 참고, 25.4%)라 이 역전의 학습 영향이 크다. **조치(F1 필터)**: `game_type=='F' & season<=2022` 행을 학습에서만 제거, 검증/추론은 원본 유지 — 2023 이후 F는 새 관계가 유효하므로 남긴다(전량 제거는 팀원 리포트 기준 2024에서 -105점 손해, 가중치 희석 방식은 조기종료가 첫 트리에서 멈춰 실패).

**16.3 트랙맨 3가지 접근 — 팀원 독립 검증, 전부 실패**: 팀원이 별도로 복원한 팀/투수 크로스워크(개막전 투구수 대조, 신뢰도 0.9999)를 이용해 (a) 상황 지문 조인(-40.4), (b) 투수 ID 기반 통산 물리량(-38~-46, feature importance는 잡혔으나 `asof_pitcher_success_rate`와 중복 정보로 순노이즈), (c) 경기 중 실시간 상태(누수 방지 `expanding().shift(1)` 설계, 최고 상관 0.0137로 이 데이터 최고 상관 0.0846의 1/6 수준 — 유의미한 신호 없음) 전부 검증, 전부 실패. §15의 우리 자체 결론(트랙맨 신호 없음)과 정확히 수렴.

**우리 아키텍처로 직접 재현**: 팀원 리포트가 우리 최신 아키텍처 기준이 아니므로 그대로 믿지 않고 `code/experiment_f1_filter.py`로 재현. 2024 단독으로는 F1이 오히려 -19.56 손해처럼 보이지만, 2023의 붕괴(72.24→550.15, **+477.91**)를 압도적으로 만회하며 **2023+2024 평균 +229.18**. **채택.**

## 17. 트랙맨 드롭 + F1 필터 — 프로덕션 반영, 부수 버그 발견

`code/train.py`/`code/test.py`/`dopip.py`/`submit/script.py`에 실제 반영 후 전체 파이프라인 재실행. **부수 버그**: `code/test.py`가 신규 모델과 기존 reference를 비교할 때 dict 키 구성만 확인하고 그 안의 실제 피처 스키마(컬럼 구성)까지는 확인하지 않아, 트랙맨 피처 제거로 스키마가 바뀌자 기존 reference의 CatBoost가 `predict_proba`에서 컬럼 불일치 예외를 던지며 파이프라인 전체가 죽음 — 기존 "레거시 포맷은 비교 건너뛰기" 로직과 같은 맥락으로 try/except 처리 추가해 수정.

실행 결과: F1 필터로 학습 데이터 105,308행 제거, **로컬 블렌드 777.61**(독립 실험값 775.57과 거의 일치, 재현 확인) — 신규 모델 승격, 전체 재학습 완료.

## 18. 2023+2024 검증 컨벤션 전환 검토 — F1 필터와의 충돌로 기각

§16.1의 "season==2024 단일 홀드아웃은 못 믿는다" 문제를, 프로덕션 스플릿 자체를 `train<2023`/`val∈{2023,2024}`로 바꿔 근본 해결할지 검토. **기각**: 진짜 듀얼 홀드아웃을 만들려면 2023을 학습에서 완전히 빼야 하는데, 그러면 남는 학습 구간(2019~2022)에 F1 필터를 적용하는 순간 F 데이터가 학습에서 **전량** 사라진다 — 이는 이미 F1보다 못하다고 확인된 "F2(전량 제거)" 방식과 동치가 된다. "검증을 제대로 하려고 2023을 떼어내면 F1 필터가 지키려던 바로 그 데이터를 잃는" 직접적 딜레마. F1 필터의 실증된 이득(+229.18)이 검증 방식 개선의 이득보다 크므로, **프로덕션 스플릿은 유지하고 2023+2024 검증은 개별 후보 변경을 판단할 때 쓰는 감사 도구(`--holdout` 플래그)로만 남긴다.**

## 19. CatBoost 재튜닝 — F1 필터로 바뀐 데이터에 맞춰 재탐색

기존 `CATBOOST_PARAMS`는 F1 필터도 트랙맨 제거도 없던 훨씬 이전 데이터 기준 튜닝값. F1 필터로 105,308행(약 8.6%)이 빠졌으니 최적값도 달라졌을 가능성을 Optuna 40 trials로 재탐색(트랙맨 없음/F1 적용된 현재 피처 구성 기준). **CatBoost 단독 689.35→721.66(+32.31)**. 반영 후 전체 파이프라인 재실행: **블렌드 777.61→794.77(+17.16)**, 신규 reference 승격, submit.zip 재구성·스모크 테스트 통과.

## 20. 실제 리더보드 971점 — 트랙맨 드롭·F1 필터·CatBoost 재튜닝 종합 확인

**결과: 971점** — 프로젝트 역대 최고 실제 제출 점수(직전 957.80417 대비 +13.2). 로컬(794.77)만 보면 §13의 879.34보다 낮아 보이지만, §15~§18에서 반복 확인했듯 season==2024 단일 지표는 이런 변화의 진짜 효과를 보여주지 못한다 — 트랙맨 드롭/F1 필터의 순효과는 2023+2024 평균으로 별도 검증됐고, 실제 대시보드가 그 검증을 최종 확인해준 셈이다. **트랙맨 완전 제거·F1 필터·CatBoost 재튜닝 모두 실사용 효과 확정.**

## 21. 트랙맨 재도입 4차 시도 — '배경 상황' 뭉뚱그린 집계도 결국 드롭 재확인

§15에서 드롭을 확정한 뒤, 완전히 다른 설계(정밀 매칭이 아니라 볼/스트라이크/아웃카운트+시즌구간(초/중/후반)+좌우타/좌우투로만 뭉뚱그린 그룹 mean/std, 신규 등판-내-투구수 `stint_pitch_no` 피처 추가)로 다시 시도했다. 2024 홀드아웃만 보면 Blend +16.82로 뚜렷한 개선처럼 보였지만, 2023 홀드아웃에서 −20.64로 뒤집혀 2023+2024 평균은 −1.91(사실상 0)이었다 — §16의 asof9key(평균 −1.35)와 같은 크기의 역전 패턴이 세 번째로 다른 설계에서도 재현됐다. **드롭 유지 재확인.** 상세: `EXPERIMENTS.md` §23.

## 22. 세 번째 모델(attention) 스크리닝 — FT-Transformer/ExcelFormer, 유망해 보였다가 앙상블에서 노이즈로 판명

CatBoost+MLP 2-way 스태킹에 구조적으로 다른 계열(self-attention 기반)을 3번째로 추가하는 실험. FT-Transformer와 ExcelFormer(semi-permeable attention을 CatBoost feature importance로 근사 구현)를 새로 작성해(`code/ft_transformer_model.py`, `code/excelformer_model.py`) 스크리닝했다. 과정에서 어텐션 모델의 검증/추론이 배치 없이 전체 검증셋을 한 번에 forward하는 잠재 버그를 발견·수정했다(seq² 스케일 메모리 — ExcelFormer는 253,507행 검증에서 실제로 약 27GB OOM을 냈고, 실전 24.6만 행 추론에서도 같은 문제가 날 뻔했다).

단일 시드 3-way 스태킹 이득은 두 시즌 모두 양수로 트랙맨과는 다른 패턴을 보였다(ExcelFormer 평균 +14.48, CatBoost 재튜닝 블렌드 이득 +17.16과 맞먹는 규모라 유망해 보였다). 그런데 7-seed 앙상블로 재검증하자 2024는 +25.63으로 더 커진 반면 2023은 −20.53으로 완전히 뒤집혔다(평균 +2.55로 축소, 이 프로젝트의 통상적 시즌 간 변동폭 안). 추정 원인: ExcelFormer 예측이 CatBoost/MLP와 상관 0.88~0.93으로 이미 높아, 시즌 하나짜리 검증셋으로 3-피처 로지스틱 회귀를 매번 새로 피팅하면 다중공선성으로 계수 부호까지 흔들린다. **드롭.** 상세: `EXPERIMENTS.md` §24.

## 23. ExcelFormer 3-way 스태킹 재검증 — 메타모델 정규화 + rolling-origin fold check로도 드롭 재확인

§22에서 남긴 가설("시즌 하나짜리 검증셋에 메타모델을 매번 새로 피팅하는 절차 자체가 불안정해 보인다, 어쩌면 그 절차 때문에 진짜 이득을 놓쳤을 수도 있다")을 실제로 고쳐서 재검증했다: `code/experiment_3way_stack.py::fit_meta_model_n`을 정규화된 `LogisticRegressionCV`로 교체하고, 시즌 2개짜리 point-check 대신 §9.1과 동일한 rolling-origin 3-fold(월 버킷 확장 윈도우, `code/experiment_attention.py`에 `--foldcheck` 옵션으로 신규 추가)로 season==2024 안에서 out-of-sample 재검증했다.

결과는 가설과 반대로 나왔다 — 3-fold 중 1승, 평균 delta −57.14 (5~6월 fold는 −140.95로 크게 나쁨). season 전체로 메타모델을 한 번만 피팅해 같은 season으로 평가하면 +6.88로 좋아 보이지만, 이는 인샘플에 가까운 낙관적 수치였다. 절차를 고치면 숨은 신호가 드러날 것이라는 가설은 기각됐고, §22의 드롭 결정이 더 강하게 재확인됐다. 상세: `EXPERIMENTS.md` §24.6.

## 24. 팀원의 독립적 트랙맨 3종 재검증 — '릴리스 산포(재현성)' 격리 테스트까지 포함해 전부 실패 재확인

사용자가 트랙맨의 `rel_speed`/`spin_rate`/release point 물리 지표, 특히 표준편차(재현성)가 `asof_*` 결과-기반 피처에는 없는 새 정보 아니냐고 이의를 제기했다. 검증 과정에서 이 리포지토리의 `build_pitcher_consistency_features`(투수·구종군별 물리 지표 std, `code/train_x30.py` 등)가 `pitcher_trackman_id`를 `pitcher_id`로 잘못 개명해 merge하는 바람에 두 ID 공간(overlap 0)이 안 맞아 항상 NaN이었다는, 이전엔 기록되지 않았던 코드 결함을 발견했다 — 이 리포지토리 코드로는 재현성 가설이 한 번도 유효하게 테스트된 적이 없었다는 뜻이다.

반면 팀원은 값 겹침이 아니라 두 파일의 볼카운트 시퀀스 전체 일치를 대조하는 방식(경기 페어링→투구수 대조→시퀀스 검증→다수결)으로 유효한 선수 단위 크로스워크를 복원했다(493명, 확신도 평균 0.9999, 커버리지 62%/67%). 이걸로 "릴리스 관련만"(산포 포함, speed/spin 등 제외) 피처를 격리해서 테스트한 결과도 −38.2(2023+2024 평균)로 여전히 손해였다. 진단: feature importance 1위 `tm_speed_mean`은 제구력 신호가 아니라 사실상 투수 개인 식별자로 쓰이고 있었고, 그 정체성 정보는 이미 `asof_pitcher_success_rate`가 더 정확한 형태(실제 결과 이력)로 담고 있어 물리 지표는 중복 노이즈가 된다 — 재현성(산포) 계열도 결국 투수 개인의 고유 특성이라 같은 함정에 빠진다.

경기 중 실시간 상태 상관도 최고 0.0137(`asof_pitcher_success_rate`의 0.0846 대비 1/6), 상황 지문 조인도 −40.4로 이 팀 자체 결과(§14.1)와 일치. 부수적으로 "선발/불펜 구분" 피처(−7.0)와 "asof 시차 보정" 피처(−39.8, F1 필터가 이미 근본 원인을 제거해 중복 노이즈)도 팀원 쪽에서 시도·기각됐다. **3가지 독립 접근 전부 실패, 재현성 가설도 기각 — 트랙맨 활용은 최종 종결.** 상세: `EXPERIMENTS.md` §25.

## 25. 2023 fold 붕괴 최종 분해 — R은 완전히 안정적, F가 100% 원인

팀원이 13단계에 걸쳐 "2023 fold가 왜 그렇게 나쁜가"를 조사했다. 핵심 발견: `game_type=='F'` 성공률만 2022 70.9%→2023 47.3%로 붕괴(§18.2와 일치)하고, 같은 투수의 F/R 겸업 비교로 "F로 못하는 투수가 몰려서"가 아니라 F라는 카테고리 자체의 효과가 사라졌음을 확인. 중간에 "2023 Brier 손실의 89.5%가 R에서 나온다"는 결과가 나와 "F만으로는 설명 안 되고 R도 무너진 것 아니냐"는 의문이 있었는데(이 세션에서 재확인을 요청), 후속 확인 결과 이는 R이 전체 데이터의 89.5%를 차지하는 데서 오는 산술적 인공물이었다 — **R의 절대 Brier(0.2485/0.2499/0.2481, 2022/2023/2024)는 세 시즌 내내 사실상 동일해 R은 레짐 시프트의 영향을 전혀 받지 않았다.**

메커니즘까지 확인됐다: `baseline_brier = r(1-r)`이 r=0.5에서 최댓값을 가지는데 2023의 전체 성공률이 마침 0.5 근방이라, 절대 예측 품질 변화 없이도 BSS(상대 점수)가 구조적으로 낮게 나온다 — §18.1의 "단일 시즌 r에 지배당한다"는 경고를 훨씬 구체적인 메커니즘으로 뒷받침한다. F 관련 상호작용 피처(`is_f × asof_pitcher_*`) 4종은 F1 필터 이후 F−R 격차가 이미 거의 0으로 수렴해 정보가 남지 않는다는 이유로 기각됐다(§14.2/§25.4의 "기존 신호끼리의 명시적 곱은 중복" 패턴과 일치). **2023 붕괴는 F로 완전히 설명되고, F1 필터가 정확히 올바른 수정이었음이 재확인됐다.** 상세: `EXPERIMENTS.md` §26.

---

## 핵심 교훈 (일반화 가능한 것들)

1. **벤치마크 숫자는 검증 방법론부터 확인하라.** Colab 1,500점은 코드 버그(무작위 분할 폴백)로 인한 무효 지표였다(§3).
2. **미래에 새 값이 나올 게 확실한 컬럼은 임베딩하지 말라.** `season`을 범주형으로 다루면 검증/실전에서 반드시 마주치는 미학습 카테고리가 예측을 통째로 망가뜨린다 — MLP 임베딩(§3.1)과 CatBoost target-statistic(§7) 둘 다에서 재현된, 모델 종류를 안 가리는 교훈.
3. **고카디널리티 ID 임베딩은 데이터 양 대비 신중하게.** 표본 부족 카테고리는 쉽게 암기된다. 다른 피처(누적 성공률)가 이미 정체성 정보를 간접적으로 담고 있다면 원본 ID의 실익이 작을 수 있다.
4. **단일 신경망 분산이 병목이면 하이퍼파라미터보다 앙상블을 먼저 의심하라.** lr/wd 조정은 무의미했지만 시드 앙상블만으로 690→790점(§3.1).
5. **앙상블도 무한정 좋아지지 않는다.** 7→15 시드, 네트워크 확장 모두 정체(§3.1) — 어느 시점부터 분산 감소보다 모델 표현력 한계가 병목.
6. **로컬 홀드아웃은 실전 순위도 절대 점수도 보장하지 않는다 — 이 프로젝트에서는 매번 실전이 로컬을 상회했다.** MLP 단독(+10), 알파 블렌드(+72.81), quantile embedding(+78.5), 이번 세션(트랙맨 드롭+F1+재튜닝, 로컬은 오히려 낮아 보였는데 +176.23 = 971-794.77). 로컬과 실전이 갈리면 실전을 신뢰할 것(§4, 반복 재확인).
7. **다른 모델 "계열" 간 블렌딩은 가성비가 매우 높다.** 오차 상관 0.999에 가까웠음에도 alpha 블렌드만으로 단독 성능을 모두 넘었다(§6). 반면 이질성은 "다른 알고리즘"이 아니라 "실제로 다르게 틀리는가"에서 나온다 — TabM은 신경망 계열이라 MLP와 상관관계가 비슷해 스태킹 이득이 없었다(§10).
8. **이질성만으로는 부족하다 — 단독 성능이 뒷받침돼야 한다. 그리고 튜닝이 역효과를 낼 수도 있다.** 상관관계가 가장 낮았던 RandomForest가 단독 성능이 약해 스태킹을 가장 크게 깎아먹었다(§11 1라운드). 반대로 XGBoost를 실전 튜닝해 더 강하게 만들자 상관관계도 같이 올라 이질성 이득이 상쇄, 오히려 손해였다(§11 2라운드).
9. **rolling-origin CV의 극단적 fold는 "그 시점의 특성"이 아니라 "표본 부족에 의한 메타모델 불안정"일 수 있다.** 학습 표본이 작은 fold에서 나온 큰 손해는, 학습 표본을 늘리면 대부분 사라졌다(§11).
10. **모델의 약점이 항상 그 모델의 한계는 아니다 — 목적함수가 평가지표와 안 맞아서일 수도 있다.** RandomForest는 objective를 Gini에서 Brier와 동일한 MSE로 바꾸는 것만으로 28% 개선됐다(§11). 다만 같은 트릭이 CatBoost(그래디언트 기반)에서는 정반대로 손해였다(§12) — 배깅과 그래디언트 부스팅에서 "objective를 평가지표에 맞추자"는 정반대로 작동한다.
11. **표본이 적어 보인다고 성급하게 버그로 판단하지 말라.** 트랙맨 매칭 지문의 세분화(median n=1)는 "노이즈"가 아니라 선수 ID 조인이 불가능한 상황에서의 의도적 근사 설계였다 — coarsening은 전부 손해였다(§12).
12. **가장 중요한 교훈(이번 세션): 검증 컨벤션 자체가 결론을 뒤집을 수 있다.** season==2024 단일 홀드아웃은 순위뿐 아니라 델타의 부호까지 뒤집었다(트랙맨 asof9key: 2024=+8.91, 2023=-29.11, 평균=-1.35). 개선처럼 보이는 결과를 프로덕션에 반영하기 전, 최소한 다른 하나의 시간 슬라이스로 방향을 재확인하는 습관이 이 프로젝트 후반부의 모든 올바른 결정(F1 필터 채택, 트랙맨 최종 드롭, 3-way 스태킹 재확인)을 가능하게 했다.
13. **검증 방식을 "더 엄밀하게" 바꾸는 것 자체가 다른 개선과 충돌할 수 있다.** 2023+2024 듀얼 홀드아웃(§18)은 이론적으로 더 안전한 검증이지만, 이미 채택된 F1 필터의 효과를 무력화한다는 부작용 때문에 프로덕션에는 반영하지 않고 감사 도구로만 남겼다 — "더 엄밀한 검증"이 항상 순수하게 우월하지는 않으며, 기존에 확정한 다른 결정과의 상호작용까지 함께 따져야 한다.
14. **"단일 시드로 좋아 보인다"는 스태킹 이득 자체에도 적용된다 — 앙상블(분산 감소)이 개별 모델 점수만이 아니라 스태킹 이득의 부호까지 바꿀 수 있다.** ExcelFormer 3-way 스태킹은 1-seed에서 두 시즌 평균 +14.48로 유망했지만 7-seed 앙상블에서 +2.55로 쪼그라들고 2023 부호가 뒤집혔다(§22). 상관관계가 이미 높은(0.88~0.93) 3번째 모델을 시즌 하나짜리 검증셋으로 스태킹하면 메타모델 계수가 다중공선성으로 불안정해지는 게 원인으로 보인다 — "이질적으로 보이는 3번째 모델"을 스태킹에 넣기 전에는 개별 solo 점수뿐 아니라 스태킹 이득 자체도 앙상블(분산 감소) 상태로 재확인해야 한다.
15. **검증 절차의 결함을 고치는 게 항상 "숨은 신호를 드러내는" 방향으로 작동하지는 않는다 — 오히려 낙관적 착시를 걷어내고 더 나쁜 진실을 보여줄 수도 있다.** ExcelFormer 3-way 스태킹은 메타모델 정규화(`LogisticRegressionCV`) + rolling-origin fold check(§23)를 적용한 뒤에도 여전히, 오히려 더 명확하게 손해였다(3-fold 중 1승, 평균 −57.14). "검증 방법이 부실해서 진짜 이득을 놓쳤을 것"이라는 가설은 그럴듯해 보여도, 실제로 절차를 고쳐 확인하기 전까지는 어느 방향으로 결론이 날지 알 수 없다 — 가설 자체도 실측으로 검증 대상이다.
16. **feature importance가 높다고 "새로운 정보"라는 뜻은 아니다 — 이미 있는 더 정확한 피처의 노이즈 낀 대리 식별자로 쓰이고 있을 수 있다.** 트랙맨 물리 지표(구속·산포 등)는 CatBoost가 실제로 활발히 사용했음에도(feature importance 15%) 점수를 깎았다 — 1위 피처(`tm_speed_mean`)가 제구력 신호가 아니라 투수 개인 식별자 역할을 했고, 그 정체성 정보는 이미 `asof_pitcher_success_rate`(실제 결과 이력)가 더 정확하게 담고 있었기 때문이다(§24). "그럴듯해 보이는 새 축의 정보"도 실은 기존 피처가 이미 더 잘 담고 있는 정보의 재포장일 수 있다 — §14.2의 단조변환 중복 피처 실패와 함께 이 프로젝트에서 두 번째로 확인된 패턴이다.
17. **BSS는 그 서브셋의 실제 성공률 r이 0.5에 가까울수록 절대 성능 변화 없이도 구조적으로 더 낮게 나온다.** `baseline_brier = r(1-r)`이 r=0.5에서 최댓값을 갖기 때문 — 2023 fold가 유독 나빠 보였던 것도 R(전체의 89.5%)의 절대 Brier는 2022/2024와 사실상 동일(0.2485/0.2499/0.2481)했는데, 2023 전체 r이 0.5 근방이라 상대 점수만 가혹하게 나온 것이었다(§25). §18.1의 "단일 시즌 r에 지배당한다"는 경고를 구체적 메커니즘으로 뒷받침한다 — 시즌 간 BSS를 비교할 때는 그 시즌의 r 자체도 같이 봐야 절대 성능 변화와 착시를 구분할 수 있다.
19. **"로컬 개선 → 실전 확인 전엔 채택 안 함" 규율이 세 번째 시도에서 처음으로 보상받았다.** MLP 하이퍼파라미터 재탐색(§30, −13.15)과 CatBoost GPU 재탐색(§32, −3.64)은 로컬은 좋아 보였지만 실전에서 반려됐다. ABS 레짐 시프트(cutoff=7, §34/§36)는 이 둘과 달리 **여러 cutoff(4~10)·여러 weight(1,5)·두 시즌(2023,2024)·CatBoost 단독과 블렌드 양쪽을 교차검증**한 뒤에야 프로덕션에 반영했고, 실전에서 +11.22로 유지됐다(971→982.22). 세 케이스를 통틀어 보면 "로컬 개선을 실전 확인 없이 못 믿는다"는 규율 자체는 옳았고, 검증을 더 폭넓게(단일 cutoff/단일 시즌이 아니라) 할수록 그 개선이 노이즈가 아니라 진짜 신호일 가능성이 커진다는 것도 함께 확인됐다.
20. **데이터 필터(F1 필터)와 fold/split 재설계는 독립적으로 안전해 보여도 조합하면 임베딩을 깨뜨릴 수 있다.** F1 필터는 "season≤2022인 F행 제거"로 프로덕션 스플릿(학습에 2023 포함)에선 안전하지만, 학습 구간이 2022 이하로만 이루어진 fold(다중시즌 OOF 스태킹 실험, §29)에서는 `game_type='F'`를 학습에서 통째로 지워버려 MLP의 `game_type` 임베딩이 검증에서 미학습 카테고리를 대량으로 만나는 사고로 이어졌다 — season을 임베딩하면 안 되는 이유(교훈 #2)와 같은 메커니즘이 전혀 다른 컬럼(`game_type`)에서, 필터와 fold 경계의 조합이라는 새로운 경로로 재현된 것. 새 fold/split을 설계할 때는 "이 학습 구간에 기존 데이터 필터가 특정 카테고리를 통째로 지우지는 않는지" 반드시 먼저 확인해야 한다.

## 26. MLP 전용 서브피처 스크리닝 — §14.2의 14개 교차 피처, MLP에서도 손해 (기각)

§23의 "트랙맨 컨텍스트 피처가 CatBoost엔 +18.18인데 MLP엔 −5.89"라는 힌트에서, "CatBoost에서 실패한 명시적 교차 피처(§14.2, asof_batter 강화+pitch-mix 교차 14개, CatBoost 단독으로만 테스트돼 -43.89/-39.06)가 MLP에서는 반대로 통할 수도 있다"는 가설을 `code/experiment_mlp_subfeature.py`로 검증했다. 원본 수식이 남아있지 않아 이름으로부터 재구성하고, 트랙맨 의존으로 보이는 2개는 순수 상황 변수로 대체했다.

3-seed MLP 스크리닝 결과 baseline 744.21 → +subfeatures 730.78로 **Delta −13.43**, CatBoost 때보다는 덜 나쁘지만 방향은 동일하게 마이너스였다. "MLP는 상호작용을 스스로 못 찾으니 명시적 교차항이 유리할 것"이라는 가설은 기각됐다 — 이 14개 조합 자체가 모델 종류와 무관하게 신호가 없는 것으로 보인다. **기각.** 상세: `EXPERIMENTS.md` §27.

## 27. MLP에서 기존 피처를 빼보는 반대 방향 — 3-seed에서 유망했다가 7-seed+2023 홀드아웃에서 완전히 뒤집힘 (기각)

"추가"가 아니라 "제거"(이 프로젝트의 유일한 성공 패턴, F1 필터와 같은 방향)로 접근을 바꿔, `add_engineered_features`의 12개를 MLP 입력에서만 빼봤다. solo MLP 점수가 아니라 **CatBoost와 블렌드한 점수**로 평가하도록 스크립트를 고쳤다(프로덕션이 실제로 최적화하는 지표는 블렌드이므로).

3-seed·holdout=2024 스크리닝에서는 solo +15.25, 블렌드 +13.60으로 이번 세션 처음으로 양쪽 다 뚜렷하게 좋아진 결과가 나왔다. 그런데 7-seed로 재검증하자 블렌드 Δ가 +13.60 → −2.47로 부호가 뒤집혔고(§22의 "시드 앙상블이 스태킹 이득의 부호를 바꿀 수 있다" 패턴 재현), 2023 홀드아웃까지 추가하자 solo −25.61 / 블렌드 −15.39로 뚜렷하게 마이너스였다. **2023+2024 평균(7-seed)은 solo −8.01 / 블렌드 −8.93으로 확실히 손해.** 3-seed·단일 시즌 결과에 대한 과신을 다시 한번 경계하게 된 사례(§17.3/§22/§23/§26과 동일 패턴, 이번엔 "제거" 방향에서도 재현). **기각 — `add_engineered_features` 12개는 MLP에서도 그대로 유지한다.** 상세: `EXPERIMENTS.md` §28.

## 28. CatBoost에서도 같은 12개 피처 제거 — 대칭 실험, 평균 −2.58로 역시 기각

같은 12개를 이번엔 **CatBoost에서** 빼고(MLP는 고정) 블렌드 점수를 비교했다. CatBoost는 시드 고정이라 MLP처럼 앙상블 크기에 따라 부호가 뒤집힐 위험이 작아, 처음부터 2024/2023 두 홀드아웃을 다 확인했다. 2024는 −6.61, 2023은 +1.45로 시즌별 부호는 갈렸지만 평균 −2.58로 마이너스 — §27(MLP 제거, 평균 −8.93)만큼 크진 않지만 방향은 같다. **CatBoost도 이미 지금 피처셋이 최적에 가깝다는 뜻. 기각, 현행 유지.** 상세: `EXPERIMENTS.md` §29.

## 29. 진짜 다중시즌 OOF 스태킹 — F1 필터와 fold 설계가 구조적으로 충돌해 이 데이터셋에서는 애초에 성립 불가능함을 확인

세션 초반 "3번째 모델을 편입하기로 결정되면 투자할 규모"라며 보류했던 방향을 3번째 모델이 전부 닫힌 뒤 실제로 실행했다. expanding-window fold(val 2021/2022/2023, `train.py`가 아니라 `code/experiment_attention.py::build_split` 재사용)마다 CatBoost+MLP(7-seed)를 새로 학습해 OOF 예측을 모으고, 74만 행짜리 메타 학습셋에 메타모델을 피팅해 프로덕션 2024 예측에 적용했더니 기존 방식보다 **−197.92**로 크게 나빴다.

그런데 원인이 "OOF 스태킹이 나쁘다"가 아니었다 — fold=2021의 MLP 점수가 정확히 0.00으로 붕괴한 걸 조사하니, **세 fold 전부 학습 구간이 2022 이하 시즌으로만 구성돼 F1 필터(season≤2022인 F행 제거)가 학습 데이터의 `game_type='F'` 행을 통째로(0개) 지워버렸다** — 반면 검증셋에는 F행이 2만5천~3만 개씩 있었다. MLP의 `game_type` 임베딩이 훈련 중 한 번도 못 본 카테고리를 검증에서 대량으로 만나 미학습(사실상 무작위) 임베딩으로 예측한 것 — `season` 임베딩 금지(핵심 교훈 #2)와 정확히 같은 메커니즘이 `F1 필터 × game_type` 조합에서 재현됐다.

더 중요한 발견: **F1 필터와 양립하는 fold가 이 데이터셋(2019~2024)에서는 프로덕션 fold(2024 검증) 하나뿐이다** — 학습 구간에 2023을 포함해야 F가 안 사라지는데, 2023을 포함하면서 2024보다 이전인 검증 시즌이 존재하지 않는다. "여러 fold로 큰 메타 학습셋을 만든다"는 OOF 스태킹의 핵심 전제 자체가 이 데이터·이 F1 필터 조합에서는 구조적으로 성립하지 않는다. **재설계 비용 대비 기대 이득이 불확실해 추가 투자하지 않는다. 프로덕션의 현재 2-way 메타모델(season==2024 단일 홀드아웃 피팅)을 유지한다.** 상세: `EXPERIMENTS.md` §30.

## 30. MLP 하이퍼파라미터 재탐색 — quantile embedding 도입 후 미검증 상태였음을 발견, Optuna 재탐색은 실제 리더보드 하락으로 기각

`code/mlp_model.py`의 `LR`/`WEIGHT_DECAY`/`DROPOUT`/`BATCH_SIZE`가 §3.1(quantile embedding 도입 이전, "lr/wd 조정 무의미"라는 결론이 나온 시점)의 값을 그대로 쓰고 있고, §13/§15의 quantile embedding 도입 이후 한 번도 재검증되지 않았음을 확인했다 — CatBoost가 F1 필터 도입 후 재튜닝으로 +32.31을 얻은 것(§21)과 같은 상황. `code/tune_mlp.py`(Optuna)로 lr/weight_decay/dropout/batch_size를 재탐색했다.

3-seed 스크리닝(20 trials)에서 최적 조합(`lr=0.0087, weight_decay=0.0083, dropout=0.488, batch_size=2048`)이 Score 809.97로 baseline(730~765대)을 뚜렷하게 상회했고, lr↑·dropout↑·batch_size↓ 방향이 20개 트라이얼 전반에 걸쳐 일관되게 나타났다. 7-seed+블렌드로 2024/2023 듀얼 홀드아웃 재검증한 결과:

| holdout | MLP solo Δ | Blend Δ |
| --- | --- | --- |
| 2024 | +39.62 | +28.23 (baseline 790.46 → tuned **818.69**) |
| 2023 | −23.01 | −10.44 |
| **평균** | +8.31 | **+8.90** |

2024는 3-seed보다 델타가 더 커졌지만(818.69는 현재 프로덕션 로컬 기준 794.77보다 +23.92) 2023은 뚜렷하게 마이너스다. 평균은 플러스이지만 스윙 폭(38.67)이 이 프로젝트에서 관찰된 시즌 간 자연 변동폭(±20~40)과 겹쳐 노이즈와 구분이 애매하다 — 다만 F1 필터도 정확히 같은 패턴(한 시즌 마이너스, 평균은 확실히 플러스, §18.3)으로 채택된 전례가 있어 완전 기각도 아니다. 경계선 결과였으므로 실제로 이 하이퍼파라미터로 `dopip.py` 전체 파이프라인을 돌려 `submit.zip`을 제출해 최종 판단했다.

**실제 리더보드 957.85 — 기존 971 대비 −13.15 하락.** 2024 단일 홀드아웃의 +23.92는 실제 개선이 아니라 노이즈였다. F1 필터(§18.3)는 같은 "경계선 패턴"에서 실전에도 유효했지만, 이번엔 반대로 뒤집혔다 — 즉 "한 시즌 마이너스·평균 플러스"라는 패턴 자체가 자동으로 안전 신호는 아니라는 뜻. **최종 결론: 기각.** `code/mlp_model.py`의 `LR`/`WEIGHT_DECAY`/`DROPOUT`/`BATCH_SIZE`를 원래 값(`0.003`/`0.01`/`0.3`/`4096`)으로, `open/reference/best_model.pkl`·`submit/model/final_retained_model.pkl`을 튜닝 이전 커밋(971점 버전)으로 되돌렸다. 상세: `EXPERIMENTS.md` §31.

## 31. 2-way 메타모델을 비선형으로 교체 — poly2/MLP/GBM 전부 명확히 기각 (경계선 아님)

§30까지 "3번째 모델을 늘리는" 방향은 전부 막혔다(§22~24 등). 이번엔 방향을 바꿔, 기존 CatBoost+MLP 2개 모델은 그대로 두고 그 둘의 예측을 결합하는 메타모델 자체를 선형 로지스틱 회귀에서 비선형으로 교체하면 이득이 있는지 검증했다 — 이 각도는 아직 제대로 시험된 적이 없었다.

`code/experiment_meta_nonlinear.py`로 `open/reference/best_model.pkl`을 재학습 없이 로드해 `[cat_pred, mlp_pred]` 2개 입력만 유지한 채 세 가지 비선형 후보(2차 다항+정규화 로지스틱, 소형 신경망, 얕은 gradient boosting)를 §9.1과 동일한 rolling-origin 3-fold로 검증했다. 결과: 세 후보 전부 fold 승수 0~1/3, 평균 delta −60~−679로 뚜렷하게 손해였고, 특히 메타 학습 표본이 가장 작은 fold에서 손해가 가장 컸다 — §12.1/§12.2에서 이미 관찰된 "메타모델 파라미터가 늘어날수록 시즌 하나짜리 학습 표본에 더 취약해진다"는 패턴이 그대로 재현됐다.

이번 결과는 F1 필터나 MLP 하이퍼파라미터 재탐색처럼 "한 시즌만 마이너스, 평균은 플러스"인 경계선 패턴이 아니라 세 후보 모두 방향이 일관되게, 그것도 크게 마이너스였다. **실제 리더보드 확인 없이 로컬만으로 기각 확정.** 프로덕션 메타모델(순수 2피처 선형 스태킹)은 변경하지 않는다. 상세: `EXPERIMENTS.md` §32.

## 32. CatBoost Optuna 재탐색 확대(GPU 가속) — "탐색·프로덕션 디바이스 불일치"라는 새로운 함정을 발견하고, 프로덕션 자체를 GPU로 전환해 채택

§21의 CatBoost 재튜닝(CPU, 40 trials, 721.66) 이후 한 번도 트라이얼 수·탐색범위가 재검토되지 않았다. 탐색 속도를 위해 `code/tune.py`에 GPU 옵션을 추가해 150 trials(탐색범위도 확대)로 재실행했더니 GPU 기준 최고 763.90(`depth=10`, 매우 낮은 learning_rate, 강한 정규화)을 찾았다. 그런데 이 params를 프로덕션이 실제로 쓰는 CPU로 재학습해 season 2024/2023에서 검증하니 **두 시즌 다 기존값보다 나빴다**(평균 −5.80) — 처음엔 기각 판단이었다.

여기서 사용자가 중요한 질문을 던졌다: "MLP도 GPU를 쓰는데, CatBoost 프로덕션 자체를 GPU로 돌리면 안 되나?" 이 질문을 계기로 "GPU가 원래 더 나쁜 모델을 만드는가" vs "탐색·배포 디바이스가 다른 게 문제인가"를 분리해서 재검증했다:

- **동일 하이퍼파라미터로 CPU vs GPU 비교(순수 디바이스 효과)**: 2024는 거의 동일(721.66 vs 721.51)했지만, 2023(학습 데이터가 더 적음)은 3-seed 전부 −31~−56으로 GPU가 일관되게 나빴다. 원인으로 추정되는 것: CatBoost GPU는 우리 eval_metric(BrierScore)을 네이티브 지원하지 않아 조기종료 판정을 5-iteration 주기로만 체크하는데, `depth=6`짜리 기존 params는 보통 400~850 iteration에서 끝나 이 체크 간격의 상대오차가 크게 작용한다.
- **신규탐색 params(매우 낮은 learning_rate라 900~1400대까지 학습)는 이 체크 간격 노이즈에 상대적으로 덜 민감**해, GPU로 일관되게 학습하면 기존 params보다 6/6 시드·시즌 조합 전부 이겼다.
- 가장 중요한 비교인 "GPU+신규params" vs "현재 프로덕션 CPU+기존params(971점의 원천)": 2024는 3개 시드 모두 뚜렷한 플러스(+7~+37), 2023은 3개 시드 모두 ±11 이내로 GPU 자체의 시드 변동폭과 비슷한 크기 — §30(한 시즌이 뚜렷하게 마이너스)과 달리 "한쪽은 확실한 개선, 한쪽은 노이즈 수준 중립"인 더 안전한 패턴이다.

`code/catboost_model.py::CATBOOST_PARAMS`를 신규탐색 값 + `task_type="GPU"`로 교체하고 `dopip.py` 전체 파이프라인(7-seed MLP + GPU CatBoost + 스태킹)을 재실행: **블렌드 Val Score 812.38**(기존 794.77 대비 +17.61), `NEW_BEST`로 승격, 전체 재학습까지 정상 완료. GPU로 학습된 모델이 GPU 없는 환경에서도 `predict_proba`가 정상 동작함을 별도로 확인(트리 구조는 디바이스 무관하게 직렬화됨)하고, `submit/script.py` 스모크 테스트와 `submit.zip` 재구성도 마쳤다.

**결론: 로컬 기준 잠정 채택 → 실제 제출 967.3619755738, 971 대비 −3.64로 근소하게 하락, 최종 기각.** 로컬은 뚜렷하게 올랐지만(+17.61) 실전은 근소하게 떨어져 §30에 이어 이번 세션 두 번째 "로컬 상승·실전 하락" 사례가 됐다(다만 하락폭은 훨씬 작음, −3.64 vs −13.15). §32.3에서 이미 "GPU 학습은 특히 데이터가 적을 때 조기종료 체크가 성글어 시드 간 변동폭이 크다"는 걸 확인했는데, 이 디바이스 자체의 학습 노이즈가 실전 단일 관측치에도 비슷한 크기로 반영돼 순수 하이퍼파라미터 개선분을 상당 부분 상쇄한 것으로 추정된다. `code/catboost_model.py`·`code/tune.py`·`open/reference/best_model.pkl`·`submit/model/final_retained_model.pkl`을 전부 971점 커밋 상태로 복원했다. 상세: `EXPERIMENTS.md` §33.

## 33. 블렌드 확률 사후 보정(calibration) — isotonic/platt 둘 다 기각

CatBoost+MLP를 어떻게 합칠지가 아니라, 이미 합쳐진 최종 확률이 잘 보정돼 있는지를 검증(재학습 불필요, §32의 캐시 재사용). isotonic은 1/3 fold, 평균 −2.91로 기각(비모수 특성상 세부 확률 곡선에 과적합). platt scaling은 3/3 fold 승리했지만 평균 +1.92(+0.01~+4.12)로 이 프로젝트에서 채택된 개선들(+14~+78)에 비해 노이즈 경계선 수준 — 지금 쓰는 선형 스태킹 자체가 이미 상당히 잘 보정돼 있어 보정으로 고칠 잔차가 거의 안 남았던 것으로 해석된다. **결론: 둘 다 기각.** 상세: `EXPERIMENTS.md` §34.

## 34. ABS(자동 볼 판정 시스템) 레짐 시프트 가설 — 2024 head 데이터 포함이 견고한 개선, cutoff=7·weight=1 채택 (§36에서 실제 리더보드 982.22로 확정)

계산된 rolling-origin fold check에서 9~10월 fold가 유독 나쁜 걸 계기로, 사용자가 두 가설을 제기했다: "가을야구(2군/신인 콜업)"와 "ABS(자동 볼 판정 시스템) 도입". 데이터로 확인한 결과 콜업 가설은 반박됐고(9월 신규 등판 투수 수가 8월과 비슷), ABS 가설은 웹 검색으로 확인됨 — **KBO는 실제로 2024시즌부터 ABS를 전면 도입**했다(2020~2023엔 퓨처스리그에서만 시범 운영). `control_success`가 스트라이크존 위치 기준 라벨이라 판정 체계가 바뀌면 라벨링 자체가 달라질 수 있고, 시즌별 성공률도 2019(0.5647)→2024(0.4861)로 단조 하락하는 정황과 부합한다.

**핵심 가설**: "2019~2023(구 판정체계) 데이터는 아무리 많아도 실제 평가(2025시즌, 2024와 같은 ABS체제)와 체제가 다르다" — 학습에 2024 데이터를 일부 포함시키면 도움될 것이라는 검증. `code/experiment_abs_regime.py`/`code/experiment_abs_mlp.py`로 2024를 월 기준(cutoff)으로 head(학습 포함)/tail(검증)로 나눠 검증:

- **CatBoost 단독, cutoff 4~10 × weight 1/5 스윕**: 가중치 없이(weight=1) 2024 head를 포함시키는 것은 cutoff 4~9 전부 플러스(+12~+72)로 매우 견고했다. 반면 2024 행에 5배 가중치(weight=5)를 주는 건 방향이 들쭉날쭉(−70~+67, 뚜렷한 추세 없음) — head 표본이 작을수록(cutoff=4,5,8) 노이즈가 증폭되는 것으로 추정.
- **MLP+블렌드 검증(3-seed 스크리닝, cutoff 4~10)**: CatBoost 단독 결론이 대체로 재현됐지만, cutoff=4,5는 블렌드에서 마이너스로 뒤집혔다(head가 아주 작을 때 CatBoost의 이득을 3-seed MLP의 시드 노이즈가 집어삼킨 것으로 추정). **cutoff=7이 전체 중 가장 크고 신뢰할 만한 이득(+58.03)**으로 확정. 같은 스윕을 weight=5로도 전체 cutoff 재실행한 결과, weight=5는 표본이 큰 cutoff(4,5,6,8)에서 전부 마이너스이고 baseline보다도 나쁨(−86~−47) — weight=5는 블렌드 기준으로 weight=1을 어떤 cutoff에서도 이기지 못해, weight=1 채택을 재확인.
- **2023 교차검증**: `season<2023`만 학습하면 F1 필터가 F행을 100% 제거하는 함정(§29와 동일 메커니즘)에 걸려, F1 필터를 정상 적용하면서(2023 head는 필터 대상 아님) 검증한 결과 head-inclusion 효과가 2023에도 재현됐다(+256/+96, F1 잔여효과 섞임 가능). `game_type='R'`만 써서 F/R 이슈를 완전히 배제해도 여전히 견고한 플러스(+65/+64)로, "같은 해 데이터 포함이 도움된다"는 F1과 무관한 일반 현상임을 확인. 다만 **4월 dip은 2024에만 있고 2023(R전용)엔 없어**, "매년 반복되는 초반 콜드스타트"보다 "2024년 4월(ABS 적응기) 고유 현상"에 가깝다는 쪽에 무게가 실린다. 9~10월 약세는 2023·2024 공통.
- **weight=5 기반 시즌별 라우팅/게이팅 제안은 검토 후 기각**: weight=5의 cutoff=9 우위가 노이즈로 보이는 데다, `game_month`가 이미 원시 입력 피처라 단일 모델도 계절 효과를 학습할 수 있고, 이 프로젝트에서 구간별 특화 모델/피처는 반복적으로 실패해왔다. 사용자가 "w=5가 9~10월에서 항상 w=1을 이긴다"는 관찰(2024 기준 사실)을 2023으로 재검증해달라고 요청 — `game_type='R'`만 쓴 2023 블렌드 재현 결과, 표본이 큰 2023 10월(n=17,638, 2024 10월의 10배)에서는 오히려 w=5가 진다(−16.60), 9~10월 묶음도 이득이 +37.66→+5.50으로 급감. "표본이 작을수록 weight=5가 유리해 보인다"는 노이즈 가설이 재확인되어 기각을 재차 확정.

**최종 채택**: `train.py`/`test.py`의 분할을 `학습 = season<2024 + (season==2024 & month<7)`, `검증 = season==2024 & month>=7`, 가중치 없음으로 변경(`dopip.py`는 항상 전체 데이터로 재학습하는 구조라 수정 불필요) → `dopip.py` 전체 재실행 → 실제 리더보드 확인까지 완료(§36). 상세: `EXPERIMENTS.md` §35, §37.

## 35. "2024만 유독 절벽" 여부 재검증 — 우리 아키텍처에서는 재현 안 됨, 팀원의 "751→642" 리포트는 데이터 leak 버그로 판명

§34에서 인용했던 "팀원 앙상블 실험, CatBoost 2023(751)→2024(642) 급락" 보고를 사용자가 직접 재검증 요청. `train<season → eval==season`(R-only, F1 트랩 회피) 방식으로 2021~2024 4개 연도를 프로덕션과 동일한 CatBoost+MLP(3-seed)+블렌드로 재현한 결과, **2024가 4개 연도 중 CatBoost·MLP·Blend 세 지표 모두에서 최고점**(Blend: 2021=519.37, 2022=600.64, 2023=557.49, **2024=797.66**)이었다 — "2024만 절벽"은 전혀 재현되지 않았고, 오히려 이 프로젝트가 처음부터 가지고 있던 §23의 역사적 2023/2024 듀얼 홀드아웃 기록(Blend 558.97/792.93, 2023/2024)과도 거의 정확히 일치했다.

사용자가 제공한 팀원의 실제 코드를 리뷰한 결과, 원인을 특정하는 버그를 발견: `train_df = df_cleaned[~is_val]`(holdout_year가 *아닌* 모든 행)를 써서, `holdout_year=2023`을 돌릴 때 데이터셋의 마지막 시즌인 **2024(미래) 데이터가 그대로 학습에 leak**된다. `holdout_year=2024`만 우연히 미래가 없어 leak이 없는 정직한 검증이 된다. 즉 **751점은 미래 데이터로 부풀려진 값이고, 642점이 leak 없는 진짜 기준선** — "2024로 갈수록 급락"이 아니라 "2023 쪽이 비정상적으로 부풀려진 것"이 맞는 해석이며, 우리 자체 재현 결과(2023 낮음/2024 높음)와 방향이 정확히 일치한다. 부차 요인으로 CatBoost `cat_features` 미지정, early stopping 없이 고정 iteration, `season` 피처 제외, 시간 순서 무시한 K-Fold도 확인했으나 방향을 만드는 주범은 leak 버그 하나다.

**결론**: 이 아키텍처에서 "2024가 예측하기 유독 어렵다"는 절벽은 없다 — 정반대다. §34의 ABS 가설(head-inclusion이 2024 후반부 예측에 도움된다는 실험)은 이와 모순되지 않지만(별개의 관찰), §34에서 보조 근거로 인용했던 "751→642" 수치는 leak 버그의 산물이므로 ABS 가설의 근거 목록에서 제외한다. 상세: `EXPERIMENTS.md` §36.

## 36. cutoff=7·weight=1 프로덕션 반영 및 실제 리더보드 확인 — 982.22, 971 대비 +11.22로 최종 채택

§34의 제안을 실제로 반영했다. `code/train.py`/`code/test.py`의 분할을 `학습=season<2024 + (season==2024 & game_month<7)`, `검증=season==2024 & game_month>=7`로 교체(`dopip.py`는 항상 전체 데이터로 재학습하므로 수정 불필요)하고 `dopip.py` 전체 파이프라인을 실행했다.

결과: 로컬 블렌드 705.34(신규) vs 644.49(기존 reference를 같은 새 검증셋으로 재평가) → **NEW_BEST 승격, +60.85**(§34에서 3-seed 스크리닝으로 추정했던 +58.03과 거의 일치 — 실제 7-seed 프로덕션 학습으로도 재현됨). CatBoost `best_iteration`이 761(구)→1025(신)로, 메타모델 가중치가 `w_cat=2.119/w_mlp=2.366`(구, MLP 우세)→`w_cat=1.988/w_mlp=1.976`(신, 균형)으로 바뀌었다. Full retrain(전체 1,369,784행)으로 `submit/model/final_retained_model.pkl` 생성, `submit.zip` 재구성 후 5행 샘플로 스모크 테스트 통과.

**실제 리더보드: 982.22**(971 대비 **+11.22**). 이 세션에서 실제 리더보드로 최종 확인한 세 번째 케이스이자, **처음으로 로컬 개선이 실전에서도 그대로 유지된 케이스**(앞선 두 케이스 — MLP 하이퍼파라미터 재탐색 §30 −13.15, CatBoost GPU 재탐색 §32 −3.64 — 는 모두 반려됨). cutoff=7·weight=1 최종 채택 확정. 상세: `EXPERIMENTS.md` §37.

**사용자가 물었던 "제출 모델 자체는 2024 전체로 재학습하는데 뭐가 바뀌는 거냐"에 대한 답**: 학습 *데이터*는 이전과 동일하게 항상 전체(2019~2024 전부)를 쓴다 — `dopip.py`의 Full Retrain 단계는 분할과 무관하게 항상 전체 데이터를 쓰는 구조이기 때문이다. 바뀌는 건 그 전체 데이터로 몇 개의 트리/epoch을 쓸지, CatBoost·MLP를 얼마 비율로 섞을지를 결정하는 **reference 모델의 학습 조건**이다 — 위에서 확인한 iteration 수·메타모델 가중치 변화가 그것이고, 이 차이가 실제 리더보드 +11.22로 이어졌다.
